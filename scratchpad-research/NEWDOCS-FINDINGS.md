# investigacion2/3/4.md — consolidated verdicts (round 2)

Status: research complete on all three; DRAFT for user. Same discipline as round 1 (real-source verified, fabrications flagged, verdict vs kit constraints: zero-dep · offline-single-file · WSL2-no-GPU headless · procedural-first). **Fabrication check: ZERO fabricated repos across all three files** (unlike investigacion.md which invented 3). Star counts mildly stale-low but honest.

## Headline (all 3 files converge on ONE architecture — which we already have)
Every file restates the same thesis: **do not design a Three.js GENERATOR — design a canonical semantic model (MEP graph / CAD-JSON) as the SOURCE OF TRUTH; the AI reasons/commands over it; deterministic engines compute; Three.js is ONLY the renderer.** This is ALREADY the kit's CAD→3D two-phase architecture (certified JSON decoupled from render) + procedural-first regeneration + the Spatial World Model (round 1 S1-S3). So the new docs are ~90% CONFIRMATION of the direction, not new subsystems. The genuinely new, actionable items are small and listed below.

## investigacion2.md — AGENTIC HARNESSES (inv2 lane) → mostly v1.19
- All systems real (BlenderMCP=`ahujasid/blender-mcp`, Infinigen, SceneSmith, SAGE, Vibe3DScene, Triplex).
- **Delta G — Three.js MCP tool surface (v1.19 HEADLINE).** Agent drives the scene through validated high-level tools (Scene/Spatial/Create/Transform/Validate/Vision/Context), each mutating op returns a snapshot `{success,new_position,cell,nearby,clearance,collisions,connections}`; AI=strategy, engine=math, no free vertex manipulation. Composes over our existing pieces: Validate=inv2 Delta B; Create=hvac-fittings+router; Spatial/Context=spatial engine; Vision=capture harness. Real refs: Pascal `@pascal-app/mcp`, Vibe3DScene. It's a whole MCP server → its own v1.19 build; the single most on-target item for the user's "improve our TOOLS" ask.
- Command-layer (`addPipe({from,to,diameter,system,routing})`) → the concrete build behind Delta G.

## investigacion3.md — ENGINES/RENDERERS (inv1 lane) → all REFERENCE-ONLY + 3 small deltas
- Godot, Bevy, Babylon.js, PlayCanvas, A-Frame, Filament, CesiumJS, Model Viewer, OpenUSD, Open3D — **every full engine = REFERENCE-ONLY. No migration is remotely warranted; we stay on three.js.** A-Frame IS three.js underneath; Babylon/PlayCanvas WebGPU add nothing three.js lacks (already Phase-2-gated under SwiftShader); Godot/Bevy scene-tree/ECS lessons = already the kit's Group nesting + data/render split; Model Viewer needs a runtime custom-element dep (violates zero-dep). CesiumJS origin-rebasing only matters >~10 km (kit is single-building, noted no-delta).
- **Delta H1 (v1.18-able, doc-only) — Filament PBR citation.** Google Filament's published "Physically Based Rendering" material/lighting spec IS the canonical model three.js `MeshPhysicalMaterial` implements. One-line REFERENCE pointer in TRACK-THREEJS materials pass / block22-23. Zero code, highest-value item in the doc.
- **Delta H2 (v1.18-able, doc-only) — LINE-ARC-LINE routing doctrine note.** "Elbows = trim the two segments + insert a known-radius arc, never an arbitrary spline." Reinforces the exact-arc rule; one line in the pipe-run/rmf-frames/hvac-fittings headers. (Already embodied by hvac-fittings + elbowAtBend — this just states it.)
- **Delta H3 (v1.19 candidate) — RMF rectangular-duct swept builder.** The doc's real geometry insight: a rectangular duct must preserve width/height/forward axes along the path or the profile twists 90°. This is exactly the twist problem the kit's `rmf-frames` (double-reflection RMF) already solves for ROUND tubes — a rect-profile variant (Shape+Extrude along RMF frames) is a small, plausible extension. Candidate, not must (kit is equipment/scene-focused, not an MEP router).

## investigacion4.md — CAD VIEWERS/KERNELS/BIM (inv1 CAD + inv4 BIM) → NO net-new adoption
- **CAD kernels/viewers (inv1): all REJECT-offline or REFERENCE.** FreeCAD/LibreCAD/Mayo/CQ-editor = desktop GUI apps (REFERENCE-ONLY, architecture study). CadQuery = Python OCCT (REJECT — strictly worse than OCCT-WASM: adds CPython/Pyodide on top of OCCT). occt-import-js (kovacsv, LGPL, slim STEP/IGES/BREP→JSON) + occt-js + opencascade.js = all OCCT-WASM → fold into the existing REJECT-offline / optional-online-track verdict (multi-MB WASM fails assertNoNetwork). JSCAD/Replicad/Online3DViewer/xeokit = round-1 verdicts stand. three-gltf-viewer = MIT dev-aid reference (we own loaders).
- **BIM (inv4): web-ifc stays the intake/export primitive (MPL-2.0).** IfcOpenShell = REFERENCE-ONLY (C++/Python, LGPL+GPL, no browser build — web-ifc exists precisely because IfcOpenShell doesn't fit the browser). web-ifc-viewer + web-ifc-three = **REJECT (both officially DEPRECATED upstream)**. xeokit-sdk + xeokit-bim-viewer = **REJECT (AGPL-3.0 license blocker)**.
- Net-new: none for the kit. Doc's real signal is architectural (canonical-model-as-truth) = already the two-phase architecture; optional REFERENCE note near the intake ladder about spatial-semantic addressing (octree/voxel/level-grid + connectors) — forward-looking, mostly inv4's BIM lane.

## MATH/QC sweep across all 3 (inv3) → small refinements, one key confirmation
- ADOPT-pure-JS (fold into existing deltas): mesh volume/area/centroid QC (extends geom-verify signedVolume — inverted-winding discriminator), convex-hull envelope (QuickHull, seeds the OBB tier), frame-twist QC CHECK (flags a rect cross-section swept on Frenet instead of RMF — a defect the integrity gate currently passes). Router pressure-loss cost term (real HVAC term, pairs with bend penalty).
- SKIP-heavy: Minkowski, OCCT-WASM kernels, octree (redundant w/ BVH), PMI/GD&T tolerances (N/A offline, no CAD-import).
- **KEY CONFIRMATION: NO physics-engine collision method appears in ANY of the 3 docs** (no Rapier/Cannon/Bevy/Godot). All converge on our AABB/OBB→BVH exact cascade → BVH clash stands unchallenged; REJECT any physics-engine dep.

## v1.18 vs v1.19 split for the new round
- **v1.18-able (small doc deltas):** H1 (Filament PBR citation), H2 (LINE-ARC-LINE note), inv3's small QC refinements (volume/area/centroid, frame-twist check) folded into the existing GATES/geom-verify deltas.
- **v1.19 candidates (need own design+build):** Delta G (MCP tool surface — headline), H3 (RMF rect-duct builder), voxel-vectorizer (round 1), IFC-export track (round 1).
- **Framing sentence for v1.19 (inv2):** "canonical semantic model as source of truth; AI commands over it; deterministic engines compute; three.js renders" — the architecture all four documents converge on and the kit already partly embodies.

## Completeness round-2 closures (whole-doc critic over all 3 files — all 13 gaps verdicted)
No new fabrications (SceneSmith=nepfaff/scenesmith ICML2026, SAGE=NVlabs/sage 2602.10116, Vibe3DScene=3DSceneAgent — all REAL, web-verified). None overturns a conclusion. Verdicts:
- **#4 Auto perf-budget gate (ADOPT — v1.18-able, the one genuinely useful catch): inv3/GATES.** The kit ALREADY records draws/tris (flow-check: draws=10/tris=2746); a deterministic threshold gate (maxDrawCalls/maxVisibleTriangles per device class, spec-declared like colorTarget.deltaE00Max) is nearly free and fits the mechanical gate. Was "punted/unowned" — now assigned to inv3 for the GATES merge.
- **#3 DXF entity-extraction spec (REFERENCE/ADAPT, gated): inv4.** LWPOLYLINE bulge→arc, ACAD_TABLE→schedule, INSERT/ATTRIB→tagged equipment, via ezdxf — the concrete "how" behind the DWG intake ladder the kit asserts. Capture behind a real "read a client DXF" need, mirroring the web-ifc intake gate. inv4 owns.
- **#1 rhino3dm — REJECT-as-dep/REFERENCE** (single-format .3dm WASM parser; not a kit need; duplicates the Online3DViewer optional-viewer note).
- **#2 NIST STEP File Analyzer — REFERENCE-only** (PMI-validation ref tied to the already-REJECTED offline-STEP/OCCT-WASM path).
- **#9 Physics-stability/settling critic — REJECT (sharpens the "no physics" line): inv2.** SAGE/SceneSmith DO invoke physics SIMULATION as an acceptance critic (does the scene settle) — distinct from collision METHOD. Kit equipment is anchored/bolted, gravity-settling irrelevant, sim non-deterministic+GPU-adjacent → REJECT. Reinforces (not contradicts) the BVH-cascade stance; stated explicitly so we don't overclaim physics is merely "absent."
- **#10 Per-system verdicts SceneSmith/SAGE/Vibe3DScene — REFERENCE-ONLY each** (their load-bearing patterns — compiler-oracle, reserve→validate→commit, VLM+critic loop — already extracted into Delta G + the spatial engine).
- **#11 PCGIntegrator3D — UNVERIFIED/likely doc-internal:** add to the §2e fabricated-name guard (resolves to nothing; ship only resolvable citations). inv2 to confirm.
- **#5 Multi-channel/G-buffer render passes (OBJECT-ID/DEPTH/NORMALS) as VLM input — REFERENCE / Phase-2 ADAPT** (headless-no-GPU + determinism cap it at Phase-2-online).
- **#6 Revit Routing-Preferences size→fitting-family table — ADAPT-concept (small): inv3.** A size-indexed data table refinement to fittingForBend; low effort, deterministic.
- **#7 GeometryCache parametric-key reuse (ELBOW_100_R150_90) — BORROW-concept, already ~covered by merge-instancing-kit** (note, don't build).
- **#8 USD-style layered non-destructive composition (Base+Engineering+AI-Proposal+User-Override) — REFERENCE/ADAPT: inv1.** The one genuinely architectural OpenUSD idea; informs the DESIGNSPEC proposal→override→regenerate layering. Not a dep; a design pattern for the spec.
- **#12 Parallel candidate-branch design-space search + rollback — ADAPT-concept, largely covered by S2 + BEST_VERSION=max(Q1,Q2,Q3);** note the parallel-branch nuance, decide in-scope vs deferred (lean deferred → v1.19).
- **#13 Landmark/relative-addressing spatial memory (NORTH_WALL, "2.3 m east of CHILLER_01") — REFERENCE, folds into S1 typed scene-graph** (low priority).
