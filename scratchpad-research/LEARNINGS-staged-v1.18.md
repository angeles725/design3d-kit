# LEARNINGS §Staged rows — queued for v1.18 (NOT yet written to live kit)

These stage into `LEARNINGS.md ## Staged` (status=new) as part of the v1.18 change, **for the user to promote** (Hard Rule 6: a run stages, the user promotes). Held here until the v1.18 PR.

## OPEN VOCABULARY QUESTION (user's call — do NOT silently mint)
inv2 proposes a NEW `how` value **`research-verify`** for rows that are research verifications rather than design-run catches. Existing `how` vocabulary is design-run-only (user-correction / pre-review-catch / improvised-win). Two rows below use `research-verify` provisionally. **The user decides**: either normalize `research-verify` into the vocabulary, or remap these to `pre-review-catch`. Not minted without approval.

## Rows (10-column §Staged schema) — from investigador2 [AGENTIC]

| date | corpus | design | pass | axis | source | how | lesson | evidence | status |
|---|---|---|---|---|---|---|---|---|---|
| 2026-08-26 | threejs-hvac-prototipos | (kit) | (research) | (citations) | investigacion.md agentic-layer verification (investigador2) | pre-review-catch | The source doc cites three agentic-3D-gen systems by names that DO NOT resolve to any real repo/paper — SCRIPT3D, ImperativeScene, Compos3D — while five others verify cleanly. Their concepts are real and trace to named systems: SCRIPT3D→SceneCraft (arXiv 2403.01248)+SEIG (2606.02580); ImperativeScene→LayoutGPT imperative paradigm + "Procedural Scene Programs" (SIGGRAPH Asia 2025); Compos3D→Infinigen (princeton-vl). Guard: the kit ships ONLY citations with a resolvable arXiv/GitHub source — never a system name lifted from a design/research doc unverified. | [session-observed] two targeted web searches per name returned no matching repo/paper; verified-clean set: SceneGenAgent 2410.21909, SpatialGrammar 2604.27555, SAGE 2602.10116, Code-as-Room 2605.18451, WorldGen 2603.22972 | new |
| 2026-08-26 | threejs-hvac-prototipos | (kit) | (research) | (architecture) | investigacion.md agentic-layer verification (investigador2) | research-verify | Across the four verified precise systems (SpatialGrammar, SAGE, Code-as-Room, SceneCraft) the load-bearing mechanism is identical: the LLM/VLM edits a compact structured representation (DSL/Scene-JSON/code); a DETERMINISTIC COMPILER turns it into geometry and returns structured constraint feedback (which objects overlap, which port is unconnected, by how much), acting as a verification ORACLE that REPORTS and never repositions; the AI never writes final raw XYZ. SpatialGrammar's 2D BEV grid is rejected for HVAC (fixed vertical axis breaks overhead racks/risers/sloped drains) — transfer the pattern with a 3D occupancy grid + real elevations. | [session-observed] SpatialGrammar arXiv 2604.27555 (compiler=oracle, agent edits DSL, converges 2-3 iters, deep-read v1); SAGE 2602.10116 (visual+physics critics, DFS collision-avoidance placer); Code-as-Room 2605.18451 (deterministic harness validation/repair) | new |
| 2026-08-26 | threejs-hvac-prototipos | (kit) | (research) | (realistic pass) | investigacion.md agentic-layer verification (investigador2) | research-verify | The proxy→realistic step is the doc's OWN diagnosed failure mode ("the model reconstructs the scene and changes positions"). Make it a transform-preserving swap: the realistic pass may only substitute proxy geometry for detailed geometry/library GLB; a mechanical check diffs center/rotation/size/ports/connections before-vs-after and FAILs on any delta>epsilon. | [session-observed] doc §Etapa 4 names this failure explicitly; Code-as-Room (arXiv 2605.18451) replaces primitives with detailed geometry while keeping prior placement fixed | new |
| 2026-08-26 | threejs-hvac-prototipos | (kit) | (research) | (spatial gate) | investigacion.md §18 (investigador2) | research-verify | The doc specifies TEN hard spatial invariants the agent cannot break — the engine answers DENIED, the AI may only SUGGEST: 001 no two physical volumes overlap; 002 unique id; 003 dimensions required; 004 no placement without spatial validation; 005 world coords authoritative; 006 MEP connections use defined ports; 007 clearance volumes respected; 008 every pipe/duct has a continuous path; 009 every occupied voxel belongs to an object; 010 spatial DB updates after every commit. These become the rule set for the deterministic clash/spatial gate (inv3 clash-detect.mjs). | [session-observed] investigacion.md §18 (~lines 5749-5787); RULE001 (pipe-through-equipment) + RULE007 (pipe-in-foreign-clearance) implemented and positive-control-validated in scratchpad-research/hvac-pipe-checks | new |
| 2026-08-26 | threejs-hvac-prototipos | (kit) | (research) | (spatial API) | investigacion.md §16-20 (investigador2) | research-verify | Spatial-grounding principle: the AI must QUERY the spatial world, never imagine XYZ. Give it navigation-instrument tools — where-am-I(SpatialWorld) / what's-around(SceneGraph) / occupied?(VoxelGrid) / fits?(AABB/OBB) / touches?(BVH) / serviceable?(Clearance) / route?(Pathfinding) — plus sector-scoped context (only objects within N m of the current task, not all 15k). Attacks "everything at 0,0,0" (space reserved before commit) and "doesn't know objects exist there" (store occupied VOLUMES not points). Maps to Delta F + the SpatialWorld DB. OWNERSHIP: overlaps inv4's spatial-engine — merged into the single spatial-thesis delta, not duplicated. | [session-observed] investigacion.md §16-20 (~lines 5679-5927), 3DGraphLLM cited for semantic relations | new |
| 2026-08-26 | threejs-hvac-prototipos | (kit) | (research) | (pipeline/CAD) | investigacion.md §refined-arch (investigador2) | research-verify | The doc proposes a PARAMETRIC CAD stage (OpenCascade/OCCT) between the MEP graph and Three.js for exact CAD geometry. KIT VERDICT (inv1 CAD-kernel slice, committed 8652a2a): REJECT as a bundled offline dependency — opencascade.js 48.9MB (7.1MB min) fails assertNoNetwork; Chili3D is AGPL. Adopt ONLY as an OPTIONAL online-only research track per the TRACK-THREEJS boundary note; NOT a core pipeline stage. | [session-observed] investigacion.md refined-architecture (~line 5870 PARAMETRIC CAD); cross-verdict with inv1 TRACK-THREEJS 8652a2a | new |
