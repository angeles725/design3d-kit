# design3d LEARNINGS ledger

Two sections, two different powers. **A run may only write to §Staged.** Nothing a run learns becomes
binding until the USER promotes it. See `references/SELF-IMPROVEMENT.md` §Hard boundary.

## Load rule (binding)

At run start (P0), read **§Active ledger ONLY**. Every Active entry with status `confirmed×N` (N≥1)
or `PROMOTE` is a BINDING house rule for the run — treat it as if written in the active TRACK file.
Active entries with status `new` are advisory. `rejected` entries are dead — do not apply.

**§Staged is invisible to a run.** It is a review queue for the user, not a rulebook. Never load it,
never cite it as authority, never let a run promote its own row.

## Entry format

One table row per learning:

| date | project | design | track | pass | origin | how | learning | evidence | status |
|---|---|---|---|---|---|---|---|---|---|

- **project** — the repo/project the run lived in. A lesson from a Three.js HVAC scene is not
  automatically law for a Blender character run; the reader must be able to see the blast radius.
- **origin** — run + lineage + attempt (e.g. `surface L2 a3`) so the claim is auditable.
- **how** — how it surfaced: `blind-review-defect` · `wasted-retry` · `mechanical-failure` ·
  `user-correction` · `pre-review-catch` · `improvised-win` (a practice the run invented that
  measurably worked — not every lesson is a failure, and a vocabulary of only failure modes forces
  a false provenance onto the ones that are not). A rule you cannot trace is a rule you cannot
  revoke.
- **learning** — one imperative sentence (a rule, not a story).
- **evidence** — file/screenshot/review pointer that proves it, graded with a certainty marker:
  `[measured]` (an artifact on disk anyone can re-open — the pointer IS the proof) >
  `[session-observed]` (really happened, but left no artifact; the observation is testimony) >
  `[inferred]` (deduced, never directly seen). **A claim with no citation takes the LOWEST
  marker.** Existing rows already use this grading informally (`measured:` / `session-observed`
  prefixes) — they remain valid as written; do NOT rewrite existing rows to the bracketed form.
- **status lifecycle** — `staged` → *(USER promotes)* → `new` → `confirmed×N` (bumped when a later
  run re-observes it) → `PROMOTE` (at ×2, flag for folding into the proper `references/` file) →
  `promoted` (folded; prune the row on the next retro) · `rejected` (disproven — keep as tombstone).

## Staged (pending user decision — NOT loaded at P0, NOT binding)

| date | project | design | track | pass | origin | how | learning | evidence | status |
|---|---|---|---|---|---|---|---|---|---|
| 2026-08-09 | threejs-hvac-prototipos | camaras-frigorificas | threejs | structural | P6 blind review, v21 | blind-review-defect | When a closing element is authored SHORTER than the hole it closes, build the make-up piece in the same function or assert the sum: `leafTop + fill >= headSoffit`. A leaf height clamped to a stated 2.10 m under a measured 2.447 m head leaves a quarter-metre hole that every console, framing and count check passes. | [measured] `runs/cold-door-closed.png`, `freezer-door-closed.png` see straight through the shut doorway; `build-viewer.py` `H = min(2.10, head - 0.10)` vs frame head at `head - FRAME_F` → 0.257 m unfilled on 4 of 4 doors | staged |
| 2026-08-09 | threejs-hvac-prototipos | camaras-frigorificas | threejs | materials | P6 blind review, v21 | blind-review-defect | Score a rendered metal against the recipe's RENDERED target, never against its albedo hex — and anchor the comparison sample by projected geometry, never by eye. Scoring lum 86 as "40 % of the authored 213" made an on-target surface look like a defect, and the sample that seemed to prove the rig innocent was a painted dielectric. | [measured] kit `brushed-stainless-recipe.md` rule 5 states the target is sRGB (86,92,97) "NOT the albedo hex #d2d6d8"; the leaf measures lum 86; the "control" at lum 194 is `EQ.enamel` (metalness 0.0); the asset's own gated master reads lum 54-55 | staged |
| 2026-08-09 | threejs-hvac-prototipos | camaras-frigorificas | threejs | materials | P6 blind review, v21 | blind-review-defect | A single material's rendered value is a function of the NORMAL, so a materials target measured on one background does not transfer to another: state which background a `colorTarget` was calibrated against. Brushed 304 calibrated over the masters' dark background reads as satin at lum 86 and as a dark door at the same value inside a pale interior. | [measured] one `EQ.steel` instance spans lum 44 (vertical return face) to 213 (horizontal bench) in one frame; masonry behind it reads 191; the viewer's rig also omits the recipe's rule-2 frontal fill | staged |
| 2026-08-09 | threejs-hvac-prototipos | camaras-frigorificas | threejs | (process) | v21 operator round | user-correction | Closing an operator complaint means re-measuring the SYMPTOM they reported, not just fixing the cause you found. A real cause was removed ("the doors look black" → a near-black gasket panel across the reveal) and the complaint was recorded as answered while the leaf itself was never measured — it still reads darker than the masonry behind it. | [measured] `design-spec.yaml` operator_amendments `metal-leaves` marked done; leaf measures lum 86 vs wall lum 191 and its own frame lum 208 in `runs/*-closed.png` | staged |
| 2026-08-09 | threejs-hvac-prototipos | camaras-frigorificas | threejs | p6-blind | P6 blind review, v21 | wasted-retry | Patch-median sampling of the gate captures is a good FILTER and a bad VERDICT: of three suspicions it raised, two died once the reference and the sample were checked, and the one that held was carried by the generator's arithmetic, not by a pixel. Treat every pixel finding as a lead that must be re-proved from the source before it is written as a defect. | [measured] `runs/progress.yaml`: 1 finding survived, 1 retracted (albedo-vs-rendered + wrong-material sample), 1 dismissed (declared unmodelled scope, not placeholders) | staged |
| 2026-08-10 | threejs-hvac-prototipos | camaras-frigorificas | threejs | p6-blind | v22 remediation | mechanical-failure | PARSE a gate artefact as part of writing it. A free-text YAML value carrying a colon (`asserts it: \`x >= y\``) is read as a nested mapping and the file stops parsing — while still looking correct to a reader and to every check that only greps it. | [measured] `runs/progress.yaml` failed `yaml.safe_load` at line 62 on the first write of the v22 update; rewritten as a block scalar. The repo already carries the at-scale version of this (31 of 68 catalog design-specs invalid while every asset gated green) | staged |
| 2026-08-10 | threejs-hvac-prototipos | condensadora | threejs | (process) | operator correction | user-correction | Build equipment as a STANDALONE MASTER before integrating it into a large scene, because a master is judged close-up on a dark ground and the scene shows the same object at a tenth of the frame — three of four defects in this asset were invisible at scene scale and obvious at master scale. | [measured] uncut fan bores, Euler-sheared blades and a collapsed fin pack all survived console-clean captures in the plant viewer and were caught in the first master render | staged |
| 2026-08-10 | threejs-hvac-prototipos | condensadora | threejs | (tooling) | v22 integration | improvised-win | Port a detailed master into a big scene by building the prototype ONCE, flattening to one geometry per material and drawing it as an InstancedMesh per material — and report a failed merge with console.error, since a silently dropped merge removes parts while leaving the console clean. | [measured] four units mesh-for-mesh = 993 draws against a 750 budget (383 for the module); flattened + instanced = 656 total, 46 for the module | staged |
| 2026-08-10 | threejs-hvac-prototipos | condensadora | threejs | materials | operator feedback | user-correction | Painted sheet is a DIELECTRIC WITH A CLEAR LAYER: `metalness 0` + `clearcoat ~0.9`. Never reach for an intermediate metalness to make paint glossy — it spends diffuse for specular and darkens every large face. `clearcoat` is headless-safe under SwiftShader; it is `transmission` that stalls the compile. | [measured] top deck luminance 162 (flat) -> 84 (metalness 0.35) -> 135 (clearcoat), highlight spread 44.9 -> 82.9 -> 82.6 | staged |
| 2026-08-10 | threejs-hvac-prototipos | (kit) | threejs | (tooling) | kit audit | pre-review-catch | When a fact is corrected, GREP THE WHOLE KIT for every other place that states it. `brushed-stainless-recipe.md` was corrected on 2026-08-09 but `TRACK-THREEJS.md` still recommended the same dead property, so it kept being copied into new assets for another day. | [measured] `scene.environmentIntensity` (a no-op in r160) still live at `references/TRACK-THREEJS.md:35` a day after the recipe was fixed | staged |
| 2026-08-10 | threejs-hvac-prototipos | (kit) | threejs | (gap) | corpus vs kit | pre-review-catch | Z-FIGHTING has a rule in the nave-panccadia corpus and NONE in this kit: separate geometry by 1-2 mm first, `polygonOffset -1/-1` only where separation is impossible, `logarithmicDepthBuffer` last (~50 % fps). A grep of the kit for `z-fight`, `coplanar` and `polygonOffset` returns nothing, so any asset built from the kit alone can repeat it. | [measured] corpus `panccadia-block39.md` §39.1-39.3 vs an empty grep over `~/.claude/skills/design3d/` | SHIPPED — rule in `references/TRACK-THREEJS.md`, DETECTOR in `library/harness/geom-verify.mjs` (`coplanarPairs`/`checkCoplanar`, 75 core cases green). Proved pair-for-pair identical to the corpus in-page hook on nave-panccadia v22 (413 meshes, 73 pairs, same worst-12) BEFORE the in-page copy was deleted, so the promotion is a move and not a rewrite. |
| 2026-08-16 | threejs-hvac-prototipos | nave-panccadia/equipos | threejs | structural | condensadora-vertical fan rotor, pre-look catch (surfaced post-retro, Fabian review) | pre-review-catch | When blade geometry is generated radially in its OWN XY plane, INDEX the N blades about `rotation.z` (the blade's normal while it is still flat) and only THEN lay the rotor down with `rotation.x = -PI/2`. three's default Euler order 'XYZ' composes as Rx·Ry·Rz, so the LAST-listed axis is applied to the vector FIRST: indexing with `rotation.y` lifts each blade out of its own plane before it is laid down, and the blades fire off as vertical spars. Invisible to console and to counts — only a capture shows it. | [measured] condensadora-vertical first triage capture: one blade standing straight up out of the roof like a mast; fixed by swapping the index axis to z; `structural-attempt1-top.png` shows 4 coherent pitched blades | staged |
| 2026-08-16 | threejs-hvac-prototipos | nave-panccadia/planta-frigorifica | threejs | (integration) | v24 cold-room shelving placement (surfaced post-retro, Fabian review) | pre-review-catch | When placing assets inside a ROOM, validate each unit's FOOTPRINT against the room's bbox, never its centre — and derive the row from the room rather than marching a typed count off an anchor point. A centre-only test passes a unit half-buried in masonry, and a hand-typed row length walks straight through the wall. | [measured] v24 first build marched 4 shelving units off the operator's pick; the room (rooms.json regions[0] REFRIGERACIÓN, bbox [20.4,27.35]..[23.55,30.50], 3.15 x 3.15 m) fits 2. Units 3 and 4 stood at x 19.62 and 18.36, through the wall at x 20.4, with clean console and green probe | staged |
| 2026-08-16 | threejs-hvac-prototipos | nave-panccadia/planta-frigorifica | threejs | (integration) | v25 racking swap (surfaced post-retro, Fabian review) | pre-review-catch | A prototype that carries its END FRAME on one edge needs N+1 frames for N instanced bays. Instancing only the bay leaves the LAST bay's beams ending in mid-air. Emit a second, frame-only prototype placed one full bay past the last bay centre, and log bays and closing frames separately so the count is auditable. | [measured] v25 racking swap: `[racking] 6 bays + 2 closing frames over runs A+B`; the master's own guard (every beam must reach its uprights) is what named the defect class | staged |
| 2026-08-16 | threejs-hvac-prototipos | nave-panccadia/equipos | threejs | materials | colorTarget sampling across three assets (surfaced post-retro, Fabian review) | improvised-win | The SAMPLING ESTIMATOR for a colour anchor depends on the SUBJECT, and copying it between assets biases the target. For a WIRE GRID take the brightest ~15% (it separates wire from the dark voids between wires); for CONTINUOUS SHEET take the MEDIAN (the bright percentile would measure the blown highlight). Verify the crop's luminance sd before trusting it: sd ~0.3-20 is one surface, sd 40-77 means the crop is mixing surfaces and the number is meaningless. | [measured] estanteria-alambre wire grid, brightest-15%: (214,214,203). condensadora-vertical sheet, median: (166,170,153) — carrying the percentile over would have biased it ~50 points light. Rejected crops measured sd 42.8/77.0 and were re-picked until sd 0.26 | staged |
| 2026-08-16 | threejs-hvac-prototipos | nave-panccadia/planta-frigorifica | threejs | (integration) | v28 build, preflight catch (Fabian) | mechanical-failure | An ESCAPED APOSTROPHE inside JS that lives in a Python template string is a silent syntax bomb. `\'` is valid inside a JS literal, but the Python string collapses it to a bare `'` on the way out and the emitted JS ends the string early: the page throws `SyntaxError: missing ) after argument list`, no canvas is built, and the generator itself exits 0. Never put an apostrophe in a JS string literal inside a generator template — reword it. | [measured] build-viewer.py JS string `...this version\'s identity feature` emitted as a bare apostrophe in v28; preflight reported "no <canvas> — the scene did not build" + 0 draws while the Python build reported EXIT=0 | staged |
| 2026-08-16 | threejs-hvac-prototipos | nave-panccadia/planta-frigorifica | threejs | (integration/capture) | v28, after removing one condenser (Fabian) | pre-review-catch | REMOVING a member from a group can break the framing of that group's dedicated view even though nothing else moved: fitView sizes on the bounding SPHERE, so a gap left in a row makes the sphere stop matching the content — worst for long, thin subjects. After ANY change to what a group contains, re-run framing-probe on every view whose subject is that group, BEFORE capturing. | [measured] removing blue unit index 0 dropped the `condensers` view to occupancy 0.184 / wellFramed:false with the remaining machines unmoved; pad 1.10->1.02 and elevation 14->16 restored ok / 0.233 | staged |
| 2026-08-16 | threejs-hvac-prototipos | nave-panccadia/equipos/racking-pallets | threejs | materials | attempt2->3 colorTarget (Fabian) | mechanical-failure | A colorTarget crop search MUST reject clipped pixels (any channel >= 254). A saturated channel is not a colour, it is the exposure running out of headroom — and it comes with sd 0.00, so it looks like the most uniform crop available and wins the search. Anchoring there freezes an artefact into the contract AND hides that the paint is blowing out. | [measured] racking attempt2: lit orange upright mean R=255 sd 0.00 across several crops; M.frame.envMapIntensity 1.4->0.95 brought it back; final anchor (250,159,66) unclipped, and the same guard produced the attempt3 blue anchor (43,149,220) sd 0.07 | staged |
| 2026-08-16 | threejs-hvac-prototipos | nave-panccadia/equipos/racking-pallets | threejs | structural | attempt3 identity guard (Fabian) | improvised-win | When a design version exists BECAUSE OF one feature, add a build guard that FAILS if that feature stops being true. Counts, console and framing all stay green while the thing the version was made for quietly disappears. | [measured] racking-pallets v3 asserts `MESH_TOP > H + 0.3` — the orange mesh MUST protrude above the rack; without it a later change to the level formula could flatten the extension to zero and every other check would still pass. Same family as the fail-loud-on-zero census guards | staged |

| 2026-08-16 | threejs-hvac-prototipos | nave-panccadia | threejs | (geometry/merge) | v29, operator spotted it by eye (Fabian) | user-correction | `setIndex(null)` DOES NOT de-index a BufferGeometry — it only drops the index while the attribute buffers still hold UNIQUE vertices, which are then read as sequential triangles, so every indexed primitive silently loses faces and renders as partial surfaces. Use `toNonIndexed()`, which expands the vertices. Reach for it wherever geometries are normalised before `mergeGeometries` (which refuses a mixed indexed/non-indexed batch — the reason the wrong call gets written). CHEAP MECHANICAL DEFENCE: assert the merged tri count is >= the sum of the input tri counts — a correct merge NEVER reduces triangles. | [measured] racking master 2818 tris with setIndex(null) vs 4226 with toNonIndexed (1408 triangles never drawn); plant scene 91,518 -> 100,697. The artefact read as a "diagonal split" on the rack deck panels (luminance 174 vs 54 across a quad diagonal). It survived TWO green gates with clean console, clean network and passing framing/perf probes, and was caught by the OPERATOR by eye. A shadow-acne hypothesis with correct arithmetic (80 m shadow camera / 2048 map = 39 mm/texel vs a 40 mm deck) was MEASURED first and only moved the split 120 -> 88, which forced the search to continue. | staged |

| 2026-08-16 | threejs-hvac-prototipos | nave-panccadia/planta-frigorifica | threejs | (integration/geometry) | v30 ducts, operator picks (Fabian) | pre-review-catch | A set of operator picks tracing an object may be its EDGES, not its centerline — GROUP AND MEASURE before threading them as a path. Threading picks in pick-order is the naive "one path" read and, if they are actually the two sides of a run, produces an object that zigzags by its own WIDTH along its whole length — which renders perfectly, keeps the console clean, and passes framing/perf/count checks. Pair the rails (constant transverse separation ≈ the declared width IS the proof the pairing is right), take the per-pair MIDPOINT as the axis, and derive junctions from where the axes cross, never by eye. | [measured] v30 HVAC ducts: 34 roof picks paired into duct A (7 pairs, mean 0.673 m) + duct B (5 pairs, 0.678 m) + tail (4 pairs) against a 0.70 m duct; wye placed at CAD (16.10,41.93) where the two derived axes cross; a >20 mm end-mismatch guard added because a wye that does not touch renders as two ducts ending near each other | staged |
| 2026-08-16 | threejs-hvac-prototipos | nave-panccadia/planta-frigorifica | threejs | materials | v30 ducts material (Fabian) | user-correction | re-confirms: the painted-sheet-is-a-dielectric rule (condensadora §Staged 2026-08-10) AND the scene-metals-render-dark row (2026-08-16). A reference that authors ductwork as BARE metal (metalness 0.88) renders near-black on its vertical faces in a scene whose environment gives them almost nothing; WHITE/galvanized sheet is painted → metalness 0 + clearcoat, which restores the light read. Do not copy a reference's metalness blindly — check the material FAMILY against the evidence photo. | [measured] duct bare-metal lum 198 top / 44 sides (split 154, near-black from the floor) -> metalness 0 + clearcoat 217/162 (split 55, reads as light sheet); matches the operator photo of white ductwork | staged |

| 2026-08-16 | threejs-hvac-prototipos | nave-panccadia | threejs | (integration/geometry) | v31 ducts height, THIRD occurrence (Fabian) | pre-review-catch | A pick's Z is the height of whatever SURFACE the ray hit, not a property of the object being placed — for a SUSPENDED object (ducts, hung units, drops) the ray usually lands on the ROOF above, so its z is the roof height. DERIVE the object's height from the structure it hangs from (roofZ(x,y) - drop - h/2) so it hangs below and follows the slope, and anchor its rods to the MEASURED roof per point (emit none, and log it, where there is no roof). Building at the pick z puts the object's top face THROUGH the roof, and a constant roof-Y for the rods leaves them poking out and floating. THIRD instance this session — condensadora P2 (z 8.446 over the roof), the 4.066 'shelf height' pick, now the ducts. | [measured] 15 duct axis picks equalled roofZ within 1-30 mm; built at pick z -> top face 0.20 m through the roof at all 15 + rods on a wrong ROOF_Y 9.62 vs real 9.48 (the floating element the operator flagged); derived height -> +0.300 m clearance at all 15, __qaBoxes highest object back to the roof 9.48 with ductwork <=9.10 | staged |

| 2026-08-16 | threejs-hvac-prototipos | nave-panccadia/planta-frigorifica | threejs | (geometry) | v32 ducts seated on deck (Fabian) | pre-review-catch | To seat an object ON a slab, derive the slab's TOP from how the slab is EXTRUDED, not from level+thickness. A slab extruded DOWNWARD from its reference cota has its walking surface AT the cota (plus any ribs), not at cota+thickness — computing the mount height as level+thickness+rib floats the object by the slab thickness with a clean console, PASS preflight and PASS framing. Verify seating by dumping both bounding boxes at that height and asserting deck_top == part_base (contact, no gap/penetration). Also: framing-probe FAILs on overlapsHUD unless run with the SAME hud=off suffix the capture uses. | [measured] duct feet floated 80 mm: DECK_TOP 3.269 (level 3.152 + 0.002 + 0.08 slab + 0.035 rib) vs real walking surface 3.19 (slab extrudes down); fixed to deck top 3.19 == duct base 3.19; bbox dump `walkable-deck y[3.15,3.19]` vs `hvac-ducts y[3.27,..]` is what exposed it | staged |

| 2026-08-16 | threejs-hvac-prototipos | nave-panccadia/planta-frigorifica | threejs | (tooling/geometry) | v33 duct interference sweep (Fabian) | mechanical-failure | An AABB interference sweep over MERGED meshes reports FALSE clashes that are indistinguishable from real ones — the AABB of a merged geometry is the box of EVERYTHING it contains (a bands/feet mesh spanning the whole 13 m network, a single wall mesh 34.7x51.6 m), so it can report a column 'pierced by its full section' when nothing touches. Believe it and you re-route a duct that never interfered. For prisms and straight runs, measure interference by POINT-SEGMENT distance against the built axes and the wall segments from the scene JSON (exact), not by bounding boxes of merged meshes. Same family as 'AABBs cannot separate overlapping surfaces from boxes' (coplanar), applied to CLEARANCE not overlap. | [measured] v33: AABB sweep = 15 clashes incl. 70 m3 vs upper floor + a column at overlap 0.393x0.377 (its whole footprint), ALL false; point-segment re-measure = 0 clashes, min clearance 1106 mm (20 columns) / 527 mm (155 wall segments) | staged |

| 2026-08-16 | threejs-hvac-prototipos | nave-panccadia/planta-frigorifica | threejs | (geometry) | v34 duct section twist (Fabian) | user-correction | A duct/tube swept with `ExtrudeGeometry` + `extrudePath` TWISTS: three.js orients the profile with Frenet frames and picks the start normal itself, so at any curve that changes plane the section rotates along the run — a straight axis still LOOKS wavy, and the profile can end up 90deg rotated (0.70 wide read as 0.70 tall). Fix the profile's 'up' EXPLICITLY: sweep with a custom frame deriving true vertical per station (side = T x up_world, up = side x T), dragging the previous side only inside a genuine vertical riser. Parallel transport is NOT the fix — it doesn't twist but leaves a segment after a ramp+turn BANKED by the ramp angle (a building duct never banks). And this presents AS the operator's 'crooked picks': here the picks were EXACT (the unexplained high z was the twisted duct's real top), and the thing to regularize was the geometry, not the coordinates. | [measured] v33 trunks measured 0.40 wide x 0.70 tall + br-N/ramp intermediate 0.685/0.609 (twist signature); parallel-transport retry banked 0.431-0.515; final per-station vertical -> every run 0.400 tall, guard silent | staged |
| 2026-08-16 | threejs-hvac-prototipos | nave-panccadia/planta-frigorifica | threejs | (tooling) | v34 section guard (Fabian) | pre-review-catch | re-confirms the merged-AABB row (v33): a geometry guard that measures the MESH BBOX flags a correct sloped/ascending run as defective, because the bbox contains the ascent as well as the section. Measure the invariant you actually mean — the PROFILE at each station, stored in the geometry at generation — not the mesh's bounding box. | [measured] v34 first section-guard flagged a correct ramp because its bbox included the 0.16 m it rises; re-written to read the per-station profile height, then silent on every run | staged |

| 2026-08-16 | threejs-hvac-prototipos | nave-panccadia/planta-frigorifica | threejs | (geometry/review) | v35 "hollow ducts" that were not hollow (Fabian) | pre-review-catch | A "it looks hollow/open" report can be a correct CLOSED solid seen from an angle that flatters its section, not missing faces — MEASURE closure before chasing a hole (signed volume > 0, zero single-triangle edges + exact expected triangle count, top-vertex normal). Here the ducts were a proper closed shell; from a TOP-DOWN view a wide-low 0.70x0.40 section shows mostly its top face and, with feet/bands around it, reads as an open channel — while the earlier TWISTED 0.40x0.70 (tall-narrow) hid its interior and read solid. So the "closed duct the operator remembered was the BROKEN one." Fix the VIEW (lower angle) or confirm the section is right; do not add geometry. Also: a manifold test that flags a shape's own seam vertices (a rounded-rect duplicates a vertex where a flat face meets the corner arc) is over-measuring — assert "no edge with a single triangle" + exact count, not raw non-manifold. | [measured] 3 closure tests green on all 7 runs; the flat-shading fix made it WORSE (deck grey 12.6->16.0%, near-white 81.2->72.1%, reads as trenches) and was reverted; v33/v34/v35 tonal split 12.6/12.0/12.0 = same, so the v33->v34 difference was PROPORTION (twist corrected), not faces; operator confirmed keep 0.70x0.40 | staged |

| 2026-08-16 | threejs-hvac-prototipos | nave-panccadia/planta-frigorifica | threejs | (geometry) | v36 restoring a look that was a bug (Fabian) | user-correction | When an operator asks for the look of an earlier version, DIFF the code before recreating it — the earlier look may have been a rendering BUG, not a declared value, and 'fixing' the bug is what removed the look. Here v33 and v35 declared the identical 0.70x0.40 section; v33's liked solid look was the Frenet twist drawing it 0.40w x 0.70h. To give it back CORRECTLY, DECLARE the tall proportion (rotate the section 90deg, keeping its AREA so airflow is unchanged), never reintroduce the twist. And when runs then have different heights, they share the BASE (rest N mm above the floor), not the axis cota. | [measured] v33/v35 both `DW_TRUNK=0.70, DH=0.40`; v33 bbox height measured 0.700 (twisted); v36 declared trunks 0.40x0.70 / branches 0.40x0.50 (areas 0.28/0.20 preserved), all runs based 0.198 m above the deck, guards green | staged |

| 2026-08-18 | threejs-hvac-prototipos | nave-panccadia/equipos/evaporador-tiro | threejs | (review) | materials a1->a3, reviewer-authored criteria | wasted-retry | EXTENDS the "a blind-review correction is a HYPOTHESIS whose cause is unreliable" rule (ROLES.md diagnostician brief · GATES.md §Self-correction loop): those constrain how a judge's output is CONSUMED, none constrains what a judge may AUTHORISE. A numeric acceptance criterion a reviewer hands back is AUTHORSHIP with the force of a spec clause: anchor it to a measurement on the SOURCE or do not write it. Three invented for one surface all misfired — and each was satisfiable by the WRONG lever, which is what makes them dangerous: they look objective, so they outrank the reference in the builder's hands. | [measured] "fin edges reading behind" (refs/field-evap-1fan.jpg bay shows a tube bundle, a plate, a contactor, a valve, no fin edges); ">=25 levels from the painted casing OR a specular falloff" -> cheapest bought by DARKENING, produced a2's median-100 dark block AND 7 geometric ribs added to fake the gradient; "median >=150 / p90 >=190" -> demands 65% of the lit casing where the source measures 45-49%. Two of the pass's two allowed retries were spent on these | staged |
| 2026-08-18 | threejs-hvac-prototipos | nave-panccadia/equipos/evaporador-tiro | threejs | (review/spec) | P3 block + structural refine-spec | blind-review-defect | Prose about what a reference SHOWS must cite the measurement, not the impression — and this binds the VALIDATOR's rewrite exactly as it binds the original author. Both hands failed the same way in one spec: the author's `references[].evidences` claimed a frame shows a ceiling unit on black brackets (it shows a stainless box on the floor beside a black-legged table), and the validator's corrective rewrite then asserted "bright galvanized backing" and "fin edges reading behind", both retracted by measurement two passes later. | [measured] refs/field-evap-blackstand.jpg contains no evaporator (reference re-marked `suitability: reject`); the "bright" backing measures 76-91 against a lit casing band of 154-213 in the same frame = 40-50%; the fin pack is FULLY OCCLUDED by the tube sheet (controlled hide/show: hiding `coil_pack` leaves the bay identical, hiding the sheet moves it 140->186) | staged |
| 2026-08-18 | threejs-hvac-prototipos | nave-panccadia/equipos/evaporador-tiro | threejs | materials | a2->a3 backing measurement | mechanical-failure | re-confirms + REFINES the Active row "anchor a colorTarget crop by projecting the part's world position to pixels, never by scanning for a hue": projection is right for an UNOCCLUDED part and wrong when anything stands in front of the surface, because the projected face includes pixels its occluders paint. Derive the specimen from the ENGINE instead — render with the object visible and hidden and take the pixels that CHANGE; that is the surface's footprint by definition. | [measured] one bay backing, three instruments: hand crops 43-45%, projected quad face 92%, engine hide/show mask 54% (p10 99 / p25 112 / median 125 / p75 162 / p90 186). The copper bundle in front of the sheet is what breaks the projected read; all three numbers looked defensible in isolation | staged |
| 2026-08-18 | threejs-hvac-prototipos | nave-panccadia/equipos/evaporador-tiro | threejs | materials | a2 arbitration | user-correction | A ratio comparison between a render and a photo must use the SAME denominator on both sides — and must state whether the specimen bias is symmetric, because usually it is not. Comparing shade-vs-shade in the photo against shade-vs-lit in the render manufactured a defect; the same denominator dissolved it. The render side can be masked exactly by the engine and a photograph's side never can, so the source figure is biased DOWN and a render sitting slightly above the band is inside the evidence, not outside it. | [measured] "backing 79% of adjacent casing (photo) vs 44% (render)" -> a defect; same denominator (the lit casing band the colorTarget already calibrates) -> source 91/213 = 43%, render 100/230 = 43%, identical. Final build: 50% by hand box, 54% by engine mask, ruled inside a 40-50% band whose source side was measured with a darker-biased instrument | staged |
| 2026-08-18 | threejs-hvac-prototipos | nave-panccadia/equipos/evaporador-tiro | threejs | materials | a2 rib exoneration, then reversal | wasted-retry | A statistic must be able to RESOLVE the feature it tests — in AXIS and in SAMPLING DENSITY — and undersampling is the quieter failure, because an axis error looks wrong when you write it down. Row means every 20 px, taken in 8 px strips, over 16 mm ribs landed between the ribs as often as on them and "proved" they contributed 2 levels; an absolute pixel diff WITH AN INVERSE CONTROL then found them in seven bands at 61-82 px pitch. Same family as the Active NDC-framing row: a check that cannot see its subject reads as evidence. | [measured] rib ON/OFF row means 112/102/88/108... vs 112/101/97/111... (2 levels, "innocent"); pixel diff ON vs OFF = 39,517 changed px, max 202, and 20,283 px over threshold 20 falling in SEVEN bands. The inverse control (hide the plate: diff mean 61) is what proved the instrument responds at all | staged |
| 2026-08-18 | threejs-hvac-prototipos | nave-panccadia/equipos/evaporador-tiro | threejs | materials | panel judge misidentification | blind-review-defect | A CLIPPED specular highlight next to a surface a critical feature names is an IDENTIFICATION defect, not a cosmetic one: it recruits the eye to the wrong surface, and a blind reader will score the wrong thing while sounding perfectly coherent. Rank it accordingly in an extension — first, not last — because P6 reference-match inherits the same confusion. | [measured] a blind panel judge described the backing as an "almost-white ribbed sheet"; the lit inner wall it actually scored measures luma 238 while `header_bay_backing` measures 87-90 (proved by the same hide/show control). The blown patch was 32.7% of a 160x180 region at >=250; after the fix, 0.00% at 250 AND at 245, and that judge's vote was recorded as void | staged |
| 2026-08-18 | threejs-hvac-prototipos | nave-panccadia/equipos/evaporador-tiro | threejs | (spec/P3) | P3 colorTarget satisfiability | pre-review-catch | A source-anchored `colorTarget` can be MATHEMATICALLY UNREACHABLE by the render it gates — check satisfiability at P3, before the build. When `srgb` comes from a source with a colour cast, compute CIEDE2000 from the target against a neutral luminance sweep; if every plausible neutral render exceeds `deltaE00Max`, the gate is authored to fail and the spec is BLOCKED. Fix by keeping the source anchor and neutralising with a DOCUMENTED correction, retaining the raw measurement beside it. Also fence what the probe cannot see: a neutral-but-muddy grey passes numerically, so "reads white, not grey" stays on the feature. | [measured] target [218,213,192] (warm, B ~ R-25) at max 8: neutral 230->9.12, 218->9.12, 210->9.05, 195->10.01, 240->9.84 — every neutral render FAILS. Grey-world neutralised to [213,213,213] (gains R 0.900 / G 0.949 / B 1.197): neutral 200-240 -> 0.00-5.97 PASS, yellow 218,213,192 -> 9.03 FAIL, bare metal -> 10.39 FAIL. Blind spot: neutral 185 -> 6.99, passes | staged |
| 2026-08-18 | threejs-hvac-prototipos | nave-panccadia/equipos/evaporador-tiro | threejs | (review/process) | materials panel escalation | improvised-win | The failing JUDGE should be the one to REQUEST the panel, and revising its own vote on new measurement is legitimate — not an appeal. The kit's "same feature fails twice -> three judges, take the majority" worked here only because the judge invoked it against itself after recognising it had already committed one of the three failure modes the rule exists to catch (demanding what the physics cannot give). Record the panel's composition AND ITS COST honestly: it returned one void vote (cast on the wrong surface) and one FAIL against a clause the source does not support, so its value came NOT from the tally but from forcing the re-measurement that corrected the asset. A 2-1 majority whose winning side contains a vote on the wrong surface is not three concordant reads. | [session-observed] `open-header-bay` failed 0.70 then 0.69; the judge asked for the panel, then re-measured the source and revised its own FAIL to 0.74 PASS; panel returned 0.74 PASS / 0.83 PASS (void — wrong surface) / 0.64 FAIL. Without the escalation and the revision the asset would have closed with a clause the reference does not support still inside it | staged |

| 2026-08-18 | threejs-hvac-prototipos | nave-panccadia/equipos/tuberia-refrigeracion | threejs | (tooling/guards) | structural, guard extension | mechanical-failure | UNIFYING TEST for a whole family: if the artifact would look IDENTICAL when the world is wrong, it is not evidence. Concretely, a guard that gates a critical feature — or whose failure would be invisible in the render — ships with a FALSIFICATION RECORD: the invariant broken UPSTREAM of the guard's inputs, the guard's real output quoted carrying BOTH measured and expected values (so the record proves a comparison ran, not that something threw), and the guard identified by QUOTING ITS ASSERTION TEXT, never a line number (a line reference goes stale while pointing at a DIFFERENT assertion and keeps the look of precision). If breaking the WORLD does not make the guard fail, the guard is not measuring the world. Ceiling to declare honestly: this makes "never seen to fail" disqualifying; it does NOT make guards verified, because the modes enumerated are written by the same hand that wrote the guard. | [measured] shipped guard `const legEmbed = DECK_TOP - (DECK_TOP - 0.010)` is algebraically 0.010 for any scene — green with every leg floating, and it derived its expectation from the same constants that placed the geometry. Three more of the family followed: a world-box frozen at creation (stale the moment a parent is transformed at integration), a scene-bound `getObjectByName('deck')`, and `deckObject || deckName || default` returning TRUE against the wrong slab — that last one was WRITTEN by the builder and READ by the orchestrator, survived both, and fell on first execution. Four defects, zero visible in any render | staged |
| 2026-08-18 | threejs-hvac-prototipos | nave-panccadia/equipos/tuberia-refrigeracion | threejs | (review) | structural a1, judge FAIL refuted | improvised-win | Every FAIL declares its TYPE. A MEASURABLE finding names the instrument that produced it and the cheapest measurement that would VOID it — and "perceptual" is not available as a relabel to dodge that. A PERCEPTUAL finding declares it has no such measurement and CITES what it was compared against (the source, or the spec's own words); perceptual findings are the half a script cannot do and must not be discouraged — they escalate to a panel and are contrasted against the source rather than argued. When a finding carries both halves, state them separately. Revising a verdict on new measurement is not an appeal and costs nothing; defending one costs a correction round. | [measured] judge FAIL 0.68 on `dark-frames-carry-the-run` refuted before any retry: deck maxY 3.187 vs frames minY 3.177 = exactly the declared 0.010 embed, rail posts identical. Both evidence strands had innocent explanations (a lit "bottom face" that cannot exist at 10 mm embed; a cone-vs-leg luminance asymmetry produced by curved vs flat faces). Judge revised 0.68 FAIL -> 0.82 PASS on its own. The perceptual clause exists because the finding that triggered the whole guard review had NO measurement that could void it | staged |
| 2026-08-18 | threejs-hvac-prototipos | nave-panccadia/equipos/tuberia-refrigeracion | threejs | (process) | whole run, orchestrator side | improvised-win | A pass's real defects surface where roles OVERLAP and check each other — a builder auditing his own output and reporting bugs against himself, a judge making its verdict refusable, an orchestrator verifying what it is handed instead of relaying it. Attribute a retro's findings to the overlap that produced them, not to a person: crediting a person turns a structural property into a personal one, and a kit cannot require talent — it can require the overlap. | [session-observed] all four guard defects surfaced across role boundaries: the builder found two while executing work he had been sent to do differently (and reported one as a bug against himself); the judge's questions opened two more that no checklist contained; the orchestrator's independent measurement killed a judge FAIL and verified a builder report rather than relaying either. No single role holds the finding | staged |
| 2026-08-18 | threejs-hvac-prototipos | nave-panccadia/equipos/tuberia-refrigeracion | threejs | (tooling/capture) | materials, stale measurement | mechanical-failure | re-confirms + EXTENDS the promoted OWNER MOMENT rule (GATES.md §Artifacts), which binds a capture to its gate by canonical NAME: bind it in TIME as well. Delete the capture targets BEFORE capturing and verify each PNG's mtime against the clock before any number is read off it — a correct filename says nothing about whether the bytes are from this build. | [measured] a `cd` persisting through a compound command made the capture fail silently; the previous run's PNGs were measured and a stale lagging-contrast figure (53%) reached a review as fresh evidence. Found and withdrawn by the builder on his own initiative; the re-measurement from scratch gave 26% against a source band of 27-45% | staged |
| 2026-08-18 | threejs-hvac-prototipos | nave-panccadia/equipos/evaporador-tiro | threejs | structural | structural a1 build | pre-review-catch | `BufferGeometryUtils.mergeGeometries` returns null when the input list MIXES indexed and non-indexed geometries, and every part vanishes with a clean console. `ExtrudeGeometry` (holed sheets, extruded annuli) is non-indexed; `Box`/`Cylinder`/`Torus`/`Tube` are indexed. De-index only the ones that HAVE an index — calling `toNonIndexed()` on an already non-indexed geometry logs a warning, and a warning is an issue in the capture sidecar. `setIndex(null)` is not a substitute. | [measured] `runs/struct-1fan/*.console.json` recorded "merge FAILED for casing — 8 part(s) would vanish"; it was visible ONLY because the merge helper reports a null return instead of adding nothing | staged |
| 2026-08-18 | threejs-hvac-prototipos | nave-panccadia/equipos/evaporador-tiro | threejs | structural | structural a1, DPR-3 crop | pre-review-catch | A tube does not TERMINATE inside a thin sheet. Assert the clearance from the curve endpoint to each wall of the enclosing solid is greater than the tube RADIUS, per axis — "the centre-line is contained" is the check that passes while half an end-cap hangs in the air. Terminate appendages in a solid with volume (a gland box, a wall, a casing mass), never in a 6 mm flange. | [measured] the wiring loom ended geometrically inside the bay flange and showed a floating open cap in the native-resolution bay crop; fixed with a gland box and a per-axis radius clearance guard | staged |
| 2026-08-18 | threejs-hvac-prototipos | nave-panccadia/equipos/evaporador-tiro | threejs | structural | structural a1 framing | pre-review-catch | Fitting a camera to the 8 corners of the subject's UNION bbox frames EMPTY SPACE when the subject is L-shaped (a body with appendages hanging off one end). Fit the corners of each MESH's own world box instead: nothing is cropped and the void is not paid for. This is the FIT counterpart of the existing rule about a framing CHECK measuring the wrong thing. | [measured] the phantom corner combining the suction tail's z with the drain's y set the distance and shrank the casing to about a third of the frame; per-mesh fitting recovered it at the same guarantee | staged |
| 2026-08-18 | threejs-hvac-prototipos | nave-panccadia/equipos/evaporador-tiro | threejs | materials | materials a1->a2, dark backing | wasted-retry | CORRECTS the track's dark-metal recipe (TRACK-THREEJS §rig): for bare metal in an OCCLUDED niche the brightness levers are ALBEDO and `envMapIntensity`, not roughness. At metalness 0.9 there is no diffuse, so the albedo tints every reflection. AND the mirror-direction fill in that same recipe OVERSHOOTS on a FLAT, low-roughness plate: the whole sheet reflects the lamp at once, so it does not light the sheet, it BLANKS it. Aim such a fill OFF the mirror direction and keep it weak; the axis that decides "does this read as metal" is the GRADIENT, not the level. | [measured] roughness 0.30 + brushed map moved the bay backing by ~0 levels; albedo + envMapIntensity 1.9->3.4 moved it. The mirror-aimed fill I added to fix "too dark" then caused "reads like the paint" (backing 221,223,225 vs casing 231,231,231) and was the dominant term: dropping envMapIntensity 1.7->0.85 moved the sheet 0.2 levels | staged |
| 2026-08-18 | threejs-hvac-prototipos | nave-panccadia/equipos/evaporador-tiro | threejs | materials | materials a2, drain pipe | pre-review-catch | A streak/brushed `roughnessMap` wraps a `TubeGeometry`'s UV into RINGS and the pipe reads as corrugated flexible conduit. A texture is only right where its parameterisation is right — give swept tubes their own untextured material instead of sharing the sheet-metal one. | [session-observed] the galvanized drain read as corrugated hose in the materials capture until it was split onto a map-less `galvPipe` material | staged |
| 2026-08-18 | threejs-hvac-prototipos | nave-panccadia/equipos/evaporador-tiro | threejs | materials | materials a2, bay density | pre-review-catch | A coverage classifier that counts "dark OR saturated" pixels measures SHADOW, not content, and is vacuous on any reference photographed in shade. Run the classifier on the SOURCE as well as the render before trusting either number, and keep a control patch on a plain area to expose the threshold. | [measured] the classifier returned 93% on `refs/field-evap-1fan.jpg` because the real bay is in shadow; restricted to saturation it discriminated (reference 43.2% vs render 29.2%, control ~7%) | staged |
| 2026-08-18 | threejs-hvac-prototipos | nave-panccadia/equipos/evaporador-tiro | threejs | materials | materials ext#2, specular blowout | pre-review-catch | Near a CLIPPED metric, a small response to a large input change is NOT evidence that the term is weak — everything inside the blob is pinned at 255, so reducing the input shrinks it only at the edges until it collapses. The decisive experiment is switching the suspected term to ZERO, not turning it down. | [measured] halving the niche lamp 0.9->0.35 moved the blown fraction 4.83%->4.17% (reads innocent); switching it off gave 0.00%. It was the entire cause | staged |
| 2026-08-18 | threejs-hvac-prototipos | nave-panccadia/equipos/evaporador-tiro | threejs | materials | materials ext#2, backing specimen | improvised-win | DERIVE a measurement specimen by rendering the mesh HIDDEN and SHOWN and keeping the pixels that CHANGE. A specimen picked by eye samples whatever the eye lands on; one built from PROJECTED geometry assumes the surface is unoccluded and sweeps in whatever is visible between the parts standing in front of it. Add a `?hide=<name>` QA hook for it — it is measurement infrastructure, not render code. | [measured] three specimens gave 45% (hand crops, sampling the dark tail), 92% (projected quad, including bright casing seen between the return bends) and 54% (hide/show mask) for the SAME surface; only the third measures what the mesh paints | staged |
| 2026-08-18 | threejs-hvac-prototipos | nave-panccadia/equipos/evaporador-tiro | threejs | materials | materials ext#2, rib removal | pre-review-catch | Changing TWO variables in one capture destroys attribution. A diff can then only say "something changed", never "this changed". Re-run with a single variable moved and the signal separates. | [measured] removing 7 ribs AND raising the roughnessMap repeat in one capture gave a single 455 px change band; repeating with only the ribs moved gave SEVEN bands at a regular 61-82 px pitch | staged |
| 2026-08-18 | threejs-hvac-prototipos | nave-panccadia/equipos/tuberia-refrigeracion | threejs | structural | structural a1 guard extension | pre-review-catch | A contact guard computed FROM the same constants that positioned the geometry cannot fail: `DECK_TOP - (DECK_TOP - 0.010) === 0.010` is true whatever the scene holds. Worse than no guard, because green reads as coverage and stops anyone asking again. Measure BUILT against BUILT — each member's world box against the reference surface's own box — and it needs three further properties, all found by RUNNING it: derive the box AT CHECK TIME (a stored number is an expectation frozen in time); keep the members IN the graph (a merge helper bakes matrices and leaves the sources ORPHANED, and an orphan's box can never move); and make the reference INJECTABLE WITHOUT A DEFAULT and with NO FALLBACK (a fallback turns a safety parameter into its opposite — asking for a surface absent from this scene silently seated against the local mock-up and returned true). | [measured] eight driven scenarios: baseline true; no reference false; unknown name false; group moved +50 mm false ("off by 50.0 mm" per leg); restored true; matching injected deck true; the same deck 100 mm up false; the same deck moved 500 m in x false (empty footprint). Complements the review-side UNIFYING TEST row | staged |
| 2026-08-18 | threejs-hvac-prototipos | nave-panccadia/equipos/tuberia-refrigeracion | threejs | materials | materials, contact-signal decision | blind-review-defect | A plausible, good-faith reason given to justify NOT doing a piece of work enters the record UNREVIEWED, because it proposes no work for anyone to check. Measure the claim before recording it, exactly as if it justified doing the work. | [measured] "the shadow map already deposits contact darkening at the leg bases" was false: a luminance column at x=1208 on `runs/mat-extra/struct-frames.png` rises monotonically 99->208 into the deck with no dip at the base, while the rail-line cones DO show one (68->14). The DECISION not to add geometry stood; the reason did not | staged |

| 2026-08-18 | threejs-hvac-prototipos | nave-panccadia/equipos/puerta-cristal-industrial | threejs | (spec/P3) | structural a1 refine-spec, scoped re-gate (live append) | pre-review-catch | A scoped re-gate on a field that governs a THRESHOLD must re-test what the threshold GOVERNS, not just the field. Correcting `complexity.tier` upward RAISES the minimum spec depth the kit checks at P3, so "does it say complex now" is the wrong check and "does this spec still pass at complex" is the right one. Corollary: if it does NOT clear the new bar, DEEPEN the spec — never put the tier back. Same shape as amendment-carries-the-grep: an edit ends where its consequences land, not at the field that was touched. FOOTNOTE on the sibling correction in the same re-gate, because it misleads: a material may stay BELOW the 0.70 confidence floor — Rule 2 waives the floor for STATED ASSUMPTIONS, not for a higher number, so the fix is to move the assumption into the entry, and raising the confidence to clear the check falsifies the datum in order to satisfy the rule that exists to protect it. | [measured] complexity.scores summed 15 while the field read `moderate` (DESIGNSPEC.md:65 bands 15-24 as complex) AND the comment beside it said "low complex"; re-tested at the new tier — 9 hierarchy nodes + 2-node context, 4 materials with evidence+source, 3 adversarial critical features, a pivot with declared real travel 0..95 rather than a nominal 90, 3 attachment contracts — clears complex depth. `handle-steel` kept confidence 0.60 with its assumptions moved into the entry | staged |

| 2026-08-18 | threejs-hvac-prototipos | nave-panccadia/equipos/puerta-cristal-industrial | threejs | materials | materials a1, specular sheen (live append) | mechanical-failure | With `transparent: true`, three multiplies the WHOLE shaded result by `opacity` — the specular lobe included — so a pane at low opacity CANNOT carry a strong highlight however reflective it is. Raising reflectivity/envMapIntensity is near-inert; the only lever that would work is the opacity that the transparency clause forbids touching. Move the highlight to a SEPARATE ADDITIVE-BLENDED layer, where added light is not scaled by the tint's alpha. GENERAL FORM, past glass: when two clauses of one feature pull on the SAME parameter, a correction aimed at either is a trap until the effect is moved to a different mechanism. Two adjacent measured findings: an envMap cannot band a FLAT pane (constant normal samples ~one texel — both attempts whitened the pane and swallowed the room, the opposite and worse failure), and an additive plane with DoubleSide sees its own back face through its front with depthWrite off, mirroring the gradient into a chevron. | [measured] reflectivity 1.0 and envMapIntensity 3->7 moved the tint delta only 21->23; the additive layer gives 123,916 px at median delta 50 / p95 94 while the tint layer stays untouched at 438,588 px, median 21, luminance 170.9->200.0 on hide/show. Judge confirmed the band is confined to the pane and passes OVER the room content, so it reads as on-glass rather than as a light shaft | staged |

| 2026-08-18 | threejs-hvac-prototipos | nave-panccadia/equipos/puerta-cristal-industrial | threejs | structural | clear-glazed-leaf, live append | improvised-win | An articulated asset's OTHER state can carry a feature the default state cannot, so a state pair gives a feature two independent chances to be legible. Judged on the closed state alone the glazing was arguable — the room read through the leaf but nothing announced a pane rather than an opening; the OPEN state settled it, because swung out the pane tints the wall behind it and shows a clean edge. Spec authors: when a feature is hard to evidence in the DEFAULT state, check whether the other state carries it BEFORE adding geometry or lighting to force it. Reviewers: score across the pair unless the clause scopes itself to one state. | [measured] closed pane: scans across and down return flat bands and a 1:1 crop shows room content meeting the jamb with no glass signature; open state shows the pane tinting the wall with a clean edge. Feature scored 0.76 PASS at structural on that basis, with the specular sheen charged to materials rather than failing the pass | staged |
| 2026-08-18 | threejs-hvac-prototipos | nave-panccadia/equipos/puerta-congelacion | threejs | materials | grain axis, judge + orchestrator both mis-measured | mechanical-failure | Before believing a texture measurement, run it on the axis where the texture should NOT be — the witness axis is what turns a number into a measurement — and DETREND both axes first, because a raw row-versus-column variance on a lit surface measures the LIGHTING GRADIENT as if it were grain. Sample at a pitch finer than the declared texture period, and distrust a period detector that returns 1 px for any smooth signal. The natural instrument for measuring texture is aliased against texture itself. | [measured] judge's own first pass: raw ratio 1.64 vs detrended 1.43 on the same patch — the raw figure was reading the leaf's top-down lighting. The orchestrator fell into the same family three times independently: sampling at 2 px against a declared 2.0 px pattern, a period detector returning 1 px for any smooth signal, and raw row-vs-column variance. Two people, three forms, one pass | staged |
| 2026-08-18 | threejs-hvac-prototipos | nave-panccadia/equipos/puerta-congelacion | threejs | materials | grain axis before/after control | improvised-win | A before/after control on the PREVIOUS artefact, run with the SAME instrument on the SAME patch, is what attributes a change to the fix. Measuring only the new artefact proves the world is now right; re-running the identical measurement on the old one proves the FIX did it — which is the only claim a correction round is entitled to make. | [measured] detrended column/row ratio on an identical leaf patch: 0.56 on the previous capture (row-dominant = horizontal grain) -> 1.43 on the new one (column-dominant = vertical). Downscale check on the same instrument: 101% of native column-variance retained at 2880->2000, ratio 1.43->1.46, so the builder's declared risk of the grain averaging to flat did not materialise. The orchestrator's independent verification established only that the CURRENT grain is vertical | staged |
| 2026-08-18 | threejs-hvac-prototipos | nave-panccadia/equipos/puerta-congelacion | threejs | structural | hinge read, attempt 1->2 | blind-review-defect | Geometry can be present, correct AND occluded by its own subject — check the sub-parts that carry the READ, not the part count. Finer than the familiar 'mounted on the hidden face': the parts that make an object recognisable can sit on the side the subject's own body hides from the gate camera while the object itself is plainly visible. COROLLARY, the same shape as the glass door's opacity: the parameter that buys the read may also be the one carrying the apparent mass, so trimming it walks back the read — trim the size, not the standoff. | [measured] hinges rebuilt with barrel, pin caps, crank and strap still read as plates because all of those sat BEHIND the far edge; moving the axis 35 mm out to the visible face is what bought the read, not the size increase. Hinge extent 3.4% -> 5.4-5.5% of leaf height against a reference band of 4.6-5.6%. Same asset had already produced the coarser version of this defect (hinges on the leaf's rear face) | staged |
| 2026-08-18 | threejs-hvac-prototipos | nave-panccadia/equipos/puerta-congelacion | threejs | (reference-intake) | hinge extent adjudication | pre-review-catch | State a reference measurement as a BAND carrying its reading error, and refuse to adjudicate differences smaller than that band. A figure hand-picked off a blurred phone still quoted as a point invites a later comparison the instrument cannot support — and the reviewer who quoted it is the one who will be asked to rule on it. | [measured] reference hinge extent 30 px on a 584 px leaf = 5.1%; a +/-3 px reading error on blurred edges puts it at 4.6-5.6%. Attempt 2 measured 5.4-5.5% and the 6-8% 'excess' was declared unresolvable on exactly that ground, so no correction round was spent on it | staged |

| 2026-08-18 | threejs-hvac-prototipos | nave-panccadia/equipos/trane-tta-condenser | catalog | structural | attempt 1, coil feature (live append) | blind-review-defect | A feature whose only VISIBLE SIGNATURE is deferred to a later pass cannot be evidenced in the earlier one — and the reviewer's correct output is to score the UNVERIFIABILITY, name the voiding measurement, and state that the geometry may be right and must not be changed on the review's account. Action is refine-spec, not refine-code: the defect is in the EVIDENCE CONTRACT. Fix by SPLITTING along the new distinction, never by loosening the threshold: one feature scoring geometry from an ISOLATED capture (subject group shown alone, everything else hidden) and one scoring the surface in the pass that owns it. Rule for spec authors: when a feature's carrier surface is deferred, either scope the feature to that pass or give the earlier pass a view that shows the geometry WITHOUT the surface — otherwise the pass produces a FAIL that no build change can fix. | [measured] every outer cabinet face flat: native 200x60 strips sd 0.00 / 0.50 / 0.30 / 0.00, min-max spanning ONE luminance level, so a coil face and a painted panel were visually identical and the clause's own named failure mode ('flat panels butted at a corner with a visible seam') was indistinguishable from a correct build. Instruction to judge geometry-not-tone was correct on its face and impossible in fact. Resolved by a new `?isolate=coil` capture for the geometry half plus a new materials feature carrying grid + metalness return + tonal ratio as ONE change | staged |
| 2026-08-18 | threejs-hvac-prototipos | nave-panccadia/equipos/trane-tta-condenser | catalog | (spec/schema) | material entries, 26-spec census (live append) | pre-review-catch | DESIGNSPEC rule 2's `source` enum (extracted|table|estimated) has NO SLOT for the commonest case in a photo-driven catalog: a value MEASURED off a reference crop. A missing enum slot is not cosmetic when it forces a spec's most reliable number to be mislabelled — here a cabinet paint measured at sd 0 on three patches across two images and two units filed as `estimated`, with its real strength carried only by the numeric confidence and the prose. Two adjacent notes: rule 1 uses high|med|low for DIMENSIONS while rule 2 uses a NUMBER for materials, and an author applied the first convention to the second; and the identical defect appeared in the sibling spec because the second was written following the first's structure — a convention broken ONCE and CLONED, so when a schema defect is found in one spec, check the siblings written from the same template. REFINED after a FOURTH instance in the same pair (the camera block: az 35 suits the SQUAT condenser and was inherited by the UPRIGHT air handler, where the 1346 mm depth dominates the silhouette and the 2.01:1 identity projects as 1.10:1): clone-propagation is dangerous in SUBJECT-DEPENDENT blocks (camera, dimensions, complexity scores, perf budget, blockout scale) and harmless in subject-independent ones (environment paths, house rules). The review test is one question per block: does this block's correct value depend on what the asset IS? If yes it must be RE-DERIVED, never inherited — and inheriting it is invisible, because a cloned camera is syntactically perfect. | [measured] orchestrator census of 26 catalog specs: 24 use enum values for `source` and numeric `confidence`; the 2 offenders were this asset and its sibling air handler, on all three counts (source, confidence, important_features shape). Two other specs had already invented out-of-enum values to fill the same gap (`measured` in condensadora, `inferred` in racking-pallets and condensadora-vertical). Verified independently: `horno-conveccion/design-spec.yaml` still fails yaml.safe_load at line 40 col 37, cause = no space after the colon in `stand_h_m:{value: 0.62, ...}` while its four neighbours have it | staged |

| 2026-08-18 | threejs-hvac-prototipos | (catalog audit) | catalog | (spec/schema) | horno-conveccion YAML, orchestrator found + judge verified (live append) | pre-review-catch | A HAND-MAINTAINED ALIGNMENT CONVENTION can be the CAUSAL MECHANISM of a syntax error, and its visual reward is what hides the error. Suspicion in a hand-aligned block belongs on what looks TOO TIDY, not on what looks messy: a column-alignment habit applied to `key: value` pairs will eventually meet a key one character too long, and the only way to hold the column is to eat the space the syntax requires. Distinct from the already-recorded 'the spec was never parsed' finding — that one is about specs nobody validated, this one is about what WRITES the error. | [measured] `horno-conveccion/design-spec.yaml` fails yaml.safe_load at line 40 col 37: `stand_h_m:{value: 0.62, confidence: med}` has no space after the colon. Verified column by column — all five braces in the block sit at column 13, and `stand_h_m` is 9 characters against neighbours at 7-8, so holding the column FORCED the space out. Inserting one space parses the whole file. Pre-existing debt in another asset, diagnosed and left untouched for the user to decide | staged |

| 2026-08-18 | threejs-hvac-prototipos | nave-panccadia/equipos/trane-tta-condenser | catalog | (spec) | rule-5 headroom decided cold, orchestrator side (live append) | improvised-win | When rule 5's five-feature ceiling is reached, decide the next move BEFORE anything is failing — and the move is usually ABSORB, not merge. The cost of merging two critical features is exactly ONE LOST FAILURE GATE, and that cost is LOWEST between features that would rise and fall together anyway and HIGHEST between causally independent ones; so a merge of independent features (top deck + base, say) lets a perfect result on one average away a failure on the other. If every pair is causally independent, no merge is good and the feature set is already MINIMAL for that asset. Declare subsystem OWNERSHIP in advance (a `feature_budget` block): a new concern enters as an additional CLAUSE in the desc of the feature that already owns its subsystem, and a concern belonging to NO listed subsystem is evidence the asset exceeded a five-gate budget — split the ASSET, not the feature set. Record rejected merge candidates WITH their reason so nobody re-proposes them. JUDGE'S CAVEAT on absorption: a clause absorbed into a multi-clause feature is scored as one number, so the lost gate becomes a reviewer judgement — the review note MUST state per-clause status explicitly, or the loss of a gate becomes the loss of a record. | [session-observed] the judge proposed merging one-top-fan-under-a-wire-guard with squat-on-visible-feet; the orchestrator rejected it as the WORST available merge (top deck vs base, maximally independent — a build with perfect guard rings sitting flush on the ground would average to PASS) and rejected merging the two coil features more firmly still, since they are in DIFFERENT passes and were split precisely because one feature could not be evidenced in the pass that scored it. `feature_budget` written into the spec with count 5 / max 5 / status spent and all five subsystems named | staged |

| 2026-08-18 | threejs-hvac-prototipos | (kit tooling) | catalog | (tooling) | gate-state coverage, judge question + orchestrator code read | pre-review-catch | `gate-state.mjs` NEVER CHECKS FEATURE COVERAGE: it contains zero references to the spec's `critical_features` and its PASS loop walks only the entries PRESENT in the review. A feature omitted from a review — deliberately, as with a pass-scoped feature, or by accident — is invisible to the deriver, so the coverage guarantee rests entirely on the reviewer and the ARTEFACT must carry the protection the tool does not. Fix: enumerate the spec's critical_features and flag any the review does not cover. | [measured] `rg -c "critical_features" assets/gate-state.mjs` exits 1 (zero matches); line 171-172 is `for (const f of j.features || []) if (f.score < f.threshold)`. Surfaced by the judge asking whether a features[] shorter than critical_features would be read as a failure, after the fifth feature of trane-tta-condenser was pass-scoped to materials | staged |
| 2026-08-18 | threejs-hvac-prototipos | (kit tooling) | catalog | (tooling) | gate-state important_average short-circuit | mechanical-failure | A DECLARED SUMMARY VALUE BEATS THE DATA IT SUMMARISES, and this is the one to fix first. `gate-state.mjs` line 175 reads `j.important_average ?? reduce(...)`, so a declared average short-circuits the computation and is never cross-checked against the entries it summarises. Unlike the coercion quirks alongside it, this is a check verifying a DECLARED INTENTION instead of the measured property — same family as the already-recorded 'a guard checks intention, not its correctness' — and it lets BAD work through rather than merely annoying good work. Fix: drop the `??` and always compute, or compute and compare, flagging any disagreement. | [measured] executed: two important_features entries at 0.10 with `important_average: 0.90` declared gives guard input 0.90 against a computed 0.10, and `0.90 < 0.65` is false — passes silently. Verified in node against the exact line | staged |
| 2026-08-18 | threejs-hvac-prototipos | (kit tooling) | catalog | (tooling) | missing-score coercion across three guards | mechanical-failure | A MISSING SCORE COERCES IN THREE DIFFERENT DIRECTIONS across three adjacent guards, so the honest way to say "this has no score" is punished in two of them and differently in each — there is no single rule, there are three that resemble each other, and whoever learns one carries the wrong intuition into the next. features[] `f.score < f.threshold`: absent key gives undefined < n = FALSE (safe), `score: null` coerces to 0 and raises a FALSE PROBLEM on correctly deferred work. important_features[] `reduce(s + f.score)`: absent key makes the sum NaN and NaN < 0.65 is false (silently safe), `null` contributes 0 and drags the average toward a FALSE FAIL. important_average `?? computed`: both absent and null fall through correctly. Emit a deferred feature with NO `score` key at all; kit-side, make a missing score an explicit DEFERRED state rather than a coerced number. | [measured] node: `undefined < 0.75` false, `null < 0.75` true, `0 < 0.75` true; sum with a missing score -> NaN; `null ?? x` -> x. Applied on trane-tta-condenser structural attempt 2: fifth feature emitted with keys ['id','threshold','deferred_to','scored','note'] and `'score' in entry` verified False on the serialised object rather than trusted from intent | staged |

| 2026-08-18 | threejs-hvac-prototipos | nave-panccadia/equipos (trane-tta-condenser + trane-twe-airhandler) | catalog | (spec/inference) | three correct measurements carrying unsupported conclusions, judge + orchestrator + builder (live append) | user-correction | ASK WHAT A NUMBER CAN DISCRIMINATE, NOT WHAT IT AGREES WITH. A correct measurement that agrees with a hypothesis is worthless as support if it could not have come out differently when the hypothesis is false — the artefact test applied to arithmetic instead of pixels. And the discriminating power is usually ASYMMETRIC: what survived in all three cases here was NARROWER and FIRMER than what was claimed — the EXCLUSIONS were real and the CONFIRMATIONS were decoration — so read a number for what it RULES OUT first. Operative: (1) declare what a number EXCLUDES before reading it as support, and if the answer is nothing, say so and go find what discriminates; (2) check whether the measured quantity was DERIVED from what it is compared against, because then agreement is a tautology and self-confirmation is indistinguishable from corroboration unless provenance is traced; (3) record a voided corroboration as WITHDRAWN rather than deleting it, and VERIFY A RETRACTION BEFORE APPLYING IT exactly as you would a claim. | [measured] three cases in one session. (a) bank height matched to 2 mm 'confirming' three faces — the available height was the author's own estimate; built internal height measured 0.775 m against the estimated 0.711, at which BOTH three and four fit, so the match was an artefact of the wrong allowance. (b) built face area 2.110 m2 reproducing the catalog figure — the height was DERIVED as area-over-width, so the area matches BY CONSTRUCTION; it confirms construction fidelity and excludes TWO faces (2 faces need a 1.091 m bank in a 0.887 m cabinet) but cannot separate three from four. (c) TWE filter row agreeing to 2.8 % 'fixing' a skirt orientation — area identity is not form identity; two orientations reach 0.752 m2 equally, and what the row really demands is a plan axis >= 1.218 m. Builder retracted (c) himself against a spec that already carried it; the retraction was verified before being applied and that verification is what showed the replacement inference was weaker than believed | staged |

| 2026-08-18 | threejs-hvac-prototipos | nave-panccadia/equipos (trane pair, blast radius across 6 specs) | catalog | (spec/materials) | pre-materials paint measurement, orchestrator measured + judge checked the radius | pre-review-catch | `colorTarget.crop` MUST NAME THE FACE, NOT JUST THE MATERIAL — and this is the specimen failure that breaks a GATE rather than merely a measurement. Flat paint patches at sd < 0.5 fall into DISTINCT populations BY FACE, because each face meets the key light at its own incidence, so a crop naming only the material lets two honest measurers land on different faces and report different dE00 for the SAME build. A scalar 'render offset per asset' does not exist; the rendered value is a property of the face sampled. Where the gate camera moves between passes, the crop must be RE-DERIVED from that pass's capture and never carried over by pixel coordinates, since a change of azimuth changes every face's incidence. Same under-specified-specimen family as a hue-picked crop grabbing the wrong material, arriving by GEOMETRY instead of by colour — but worse in kind, because dE00 is a gate number: an under-specified crop does not define a test, it defines a family of tests. REFINED: there are TWO ways to make a crop deterministic and the geometry decides which — a FLAT FACE is fixed by LOCATION (name the face, one incidence follows), a CYLINDER cannot be fixed by location because it has no flat band anywhere and must be fixed by DERIVATION (a procedure anyone can repeat: hide/show mask, then the most uniform rectangle contained in it). So diagnosing a curved subject as 'the same defect in continuous form' is right and leads to the WRONG remedy: a cylinder crop is not fixed by naming WHERE, it is fixed by naming HOW. THIRD FAILURE MODE, distinct from under-specification: the procedure may EXIST but be recorded in the review instead of the spec — deterministic in the wrong artefact, since the spec is what the next measurer reads. State in the crop field WHICH mode it uses, because prose alone cannot tell a location spec from a hand-choice. And forbid the tempting false fix: narrowing the band by hand to kill a high sd swaps a DECLARED gradient for an UNDECLARED choice. | [measured] trane-tta-condenser: flat populations at 1.261/1.261/1.261/1.267 and 1.051 of target — spread 0.216. trane-twe-airhandler: 1.276/1.276/1.282 and 1.419/1.426 — spread 0.150. The between-asset difference first reported (1.27 vs 1.30, gap 0.032) is swallowed by the WITHIN-asset spread by a factor of 5 to 7, so it was an artefact of which face each crop happened to land on. BLAST RADIUS into already-closed assets, checked by the judge across all six specs: puerta-cristal-industrial reads 'a flat lit band on the frame jamb OR head' — two members with different incidences named in one field, the clearest instance; tuberia-refrigeracion reads 'a flat lit band on the main run', and a cylinder has no flat band at all (acknowledged at the time, crop sd 6.1). evaporador-tiro ('front-face band') and puerta-congelacion ('the leaf face') do name a face. CORRECTION to the judge's radius call: the pipe's crop was NOT undefined — its review records the derivation ('hide/show mask over the gate view, then the most uniform rectangle wholly inside it; not chosen by eye'), which is deterministic by procedure; the spec text was loose while the measurement was reproducible, so the fix was to LIFT the procedure from the review into the spec. RECOMMENDATION: do NOT re-open the closed gates — the builds passed on legitimate measurements — but tighten the crop text in those two specs so a future re-measure is reproducible; it is a documentation fix, not a re-gate. | staged |

| 2026-08-18 | threejs-hvac-prototipos | nave-panccadia/equipos (trane pair) | catalog | (spec/process) | shared-paint rule vs per-asset gate, conflict settled pre-evidence | improvised-win | WHEN TWO RULES CONFLICT, CHECK WHICH ONE CONSTRAINS THE MEANS AND WHICH THE END — a rule written to protect a fact can contradict that fact once it is expressed as a constraint on the MECHANISM. Sibling assets sharing one real-world paint were governed by 'both must carry the same albedo VALUE, divergence is a defect'. But what the reference establishes is a shared APPEARANCE (one paint, measured at sd 0 on both units); the albedo is only the MEANS of showing it. If two assets need different albedos to land on the same target, forcing a common albedo makes them SHOW DIFFERENT PAINTS — contradicting the very fact the rule exists to preserve. So the shared-VALUE rule is a detector of accidental DRIFT, not the real constraint, and it needs an exception clause: divergence is permitted exactly when it is NECESSARY to put both on the same target — both dE00 inside tolerance AND the divergence documented with the measurement that forced it. Divergence without that record is drift, and drift is the defect. Same family as a reviewer's invented threshold standing in for a read: when the proxy and the goal disagree, the PROXY yields. Settle such a conflict BEFORE the rule becomes blocking, because the moment it blocks is the moment the wrong resolution gets chosen under pressure. | [measured] trane pair: one real paint measured 132,135,129 and 131,133,128 at sd 0 across two reference images and two units. Condenser over-renders 1.261-1.267 on its named face, air handler 1.276-1.282 on its own — a 1.5 percent difference, so a shared albedo divided by ~1.25 predicts the air handler landing near 1.023x target. The conflict was identified and resolved BEFORE that evidence existed and while the rule was still advisory; the prediction was explicitly NOT treated as resolving the open branch. Validator implements it as: block if both passes closed and NOT both inside tolerance; advisory-with-note if they diverge while both are inside | staged |

| 2026-08-18 | threejs-hvac-prototipos | (kit tooling / equipos validator) | catalog | (tooling) | validate-specs pass detection, found at condenser close | mechanical-failure | A SORT KEY VALID WITHIN A GROUP CAN BE INVALID ACROSS GROUPS, and the comment defending it can be TRUE IN ITS OWN SCOPE while false in the scope it is applied to. Picking 'the latest PASS review' by FILENAME order returns the wrong pass, because `materials-attempt1` sorts alphabetically BEFORE `structural-attempt2` — later attempts do sort after earlier ones WITHIN a pass, which is what the comment said and what makes it convincing, but passes are ordered by the LADDER and not by their names. Use an explicit ladder rank whenever ordering crosses a grouping boundary, and read any 'sorts after' comment as a claim about a SCOPE — then check that the scope it is true in is the scope the code uses. | [measured] the orchestrator's own validator kept reporting 'materials is still OPEN on: trane-tta-condenser' after materials had closed. Old logic (filename order) picked structural-attempt2 -> pass='structural' -> materials-closed FALSE; new logic (explicit ladder rank) picks materials-attempt1 -> pass='materials' -> TRUE. CONSEQUENCE: the shared-paint divergence check could NEVER have become blocking, because it would always have believed materials was still open — a guard that cannot fire is a comment, which is the same rule the same session had already staged about a vacuous embed guard. Fixed with an explicit ladder order and demonstrated by running both logics side by side on the same asset; found by its author while closing an unrelated pass | staged |

_Queue cleared to Active 2026-08-09 (user-authorized promotion). New runs stage here again._
_Queue cleared to Active 2026-08-16 (user-authorized promotion — 9 rows from the video-additions retro)._
_18 more rows staged 2026-08-16 (post-retro: Fabian's review + the v28-v36 rounds) — awaiting the next user promotion._
_8 more rows staged 2026-08-18 (evaporador-tiro P8 retro, REVIEW-side lessons, written by the run's blind judge — see `disenos/nave-panccadia/equipos/evaporador-tiro/runs/2026-08-18-retro.md`; the builder's own inventory is separate and not yet staged)._
_1 row staged 2026-08-18 on INFERENCE FROM DERIVED NUMBERS (ask what a number can discriminate) — recommended for promotion AHEAD of the tooling rows: those are local to gate-state.mjs, this one governs how every derived number in every spec is read._
_3 TOOLING rows staged 2026-08-18 (gate-state.mjs: no coverage check · declared important_average short-circuits its own data · missing score coerces three different ways). Unifying note for the whole day: THE ARTEFACT MUST CARRY THE PROTECTION THE TOOL DOES NOT._
_4 rows LIVE-STAGED 2026-08-18 at first surprise (trane-tta-condenser, retro IN PROGRESS; one is a catalog-wide schema finding, not this asset's — see `disenos/nave-panccadia/equipos/trane-tta-condenser/runs/2026-08-18-retro.md`)._
_5 rows staged 2026-08-18 for the two DOOR assets (puerta-cristal-industrial finalised, puerta-congelacion P8; 3 of them live-staged at their surprise). Unifying note: both HIGH deltas are about INSTRUMENTS, not about doors._
_(earlier) 2 rows LIVE-STAGED 2026-08-18 at first surprise (puerta-cristal-industrial, retro then IN PROGRESS — see `disenos/nave-panccadia/equipos/puerta-cristal-industrial/runs/2026-08-18-retro.md`)._
_4 more rows staged 2026-08-18 (tuberia-refrigeracion P8 retro — GUARD-side lessons: see `disenos/nave-panccadia/equipos/tuberia-refrigeracion/runs/2026-08-18-retro.md`. Unifying test across all four: if the artifact would look identical when the world is wrong, it is not evidence.)_
_11 more rows staged 2026-08-18 (evaporador-tiro + tuberia-refrigeracion, BUILDER-side lessons written by the run's modeller — the judge's review-side and guard-side rows are staged separately above)._

_v1.18 multi-session research effort (2026-08-26) — the six rows below capture research-verification lessons from auditing investigacion.md + investigacion2/3/4.md. VOCABULARY NOTE for the user: investigador2 proposed a new `how` value `research-verify` for research verifications (vs design-run catches); to avoid minting vocabulary without your call, they are recorded here as `pre-review-catch` (closest existing) — promote `research-verify` into the vocabulary, or keep this mapping, on review._

| 2026-08-26 | threejs-hvac-prototipos | (kit) | (research) | (citations) | investigacion.md agentic-layer verification (v1.18, inv2) | pre-review-catch | The source doc cites three agentic-3D-gen systems by names that DO NOT resolve to any real repo/paper — SCRIPT3D, ImperativeScene, Compos3D (+ PCGIntegrator3D in investigacion2). Their concepts are real and trace to named systems: SCRIPT3D→SceneCraft (2403.01248)+SEIG; ImperativeScene→LayoutGPT + "Procedural Scene Programs" (SIGGRAPH Asia 2025); Compos3D→Infinigen. GUARD: the kit ships ONLY citations with a resolvable arXiv/GitHub source, never a system name lifted from a doc unverified. | [session-observed] targeted web searches per name returned nothing; verified-clean: SceneGenAgent 2410.21909, SpatialGrammar 2604.27555, SAGE 2602.10116, Code-as-Room 2605.18451, WorldGen | staged |
| 2026-08-26 | threejs-hvac-prototipos | (kit) | (research) | (architecture) | investigacion agentic verification (v1.18, inv2) | pre-review-catch | Across the verified precise systems the load-bearing mechanism is identical: the LLM/VLM edits a compact structured representation (DSL/Scene-JSON/code); a DETERMINISTIC COMPILER turns it into geometry and returns structured constraint feedback, acting as a verification ORACLE that REPORTS and NEVER repositions; the AI never writes final raw XYZ. Reject SpatialGrammar's 2D BEV for HVAC (fixed vertical axis breaks overhead racks/risers/sloped drains) — transfer with a 3D occupancy grid + real elevations. | [session-observed] SpatialGrammar 2604.27555, SAGE 2602.10116, Code-as-Room 2605.18451 deep-read | staged |
| 2026-08-26 | threejs-hvac-prototipos | (kit) | (research) | (realistic pass) | investigacion agentic verification (v1.18, inv2) | pre-review-catch | The proxy→realistic step is the doc's OWN diagnosed failure mode ("the model reconstructs the scene and changes positions"). Make it a transform-preserving swap: the realistic pass may only substitute proxy geometry for detailed geometry/library GLB; a mechanical check diffs center/rotation/size/ports before-vs-after and FAILs on any delta>epsilon. | [session-observed] doc §Etapa 4 names this explicitly; Code-as-Room (2605.18451) replaces primitives while keeping prior placement fixed | staged |
| 2026-08-26 | threejs-hvac-prototipos | (kit) | (research) | (spatial gate) | investigacion §18 (v1.18, inv2) | pre-review-catch | The doc specifies TEN hard spatial invariants the engine enforces (AI may only SUGGEST): 001 no two physical volumes overlap; 002 unique id; 003 dims required; 004 no placement without spatial validation; 005 world coords authoritative; 006 MEP connections via defined ports; 007 clearance volumes respected; 008 every pipe/duct has a continuous path; 009 every occupied voxel belongs to an object; 010 spatial DB updates after every commit. These are the rule set for the clash/spatial gate (`clash-detect.mjs`). | [session-observed] investigacion.md §18; RULE001/007 implemented + positive-control-validated in the E4/A3 verify.mjs | staged |
| 2026-08-26 | threejs-hvac-prototipos | (kit) | (research) | (spatial engine) | A1/A2/A3/E4 controlled A/Bs (v1.18, inv4+creadores) | pre-review-catch | MEASURED across four A/Bs, the doc's blanket "AI can't hold space" resolves into exact regimes: FALSE single-agent (A1 N=4, A2 N=12 both NULL — naive opus places cleanly WITHOUT the engine, matching it); a real QUALITY improver single-agent-with-a-fixed-obstacle (A3: naive 8.5/0-hard/3-soft ate ~0.75m of a unit's service clearance, engine 10/0-soft reserved the clearance volume); REQUIRED for correctness multi-agent (E4: two blind agents' primaries collide → naive-parallel 2 overlaps/6.0/FAIL vs ReserveEngine-lock 6/6/10.0/PASS — a pure observability limit no stronger model reasons away). S2 label: guarantee+clearance-awareness single-agent, required multi-agent. | [measured] exercise-A1/A2/A3/E4 RESULTS.md, scored via shared verify.mjs incl. pipe-vs-solid RULE001/007 | staged |
| 2026-08-26 | threejs-hvac-prototipos | (kit) | (research) | (geometry) | investigacion.md CAD-kernel audit + full-chain PoC (v1.18, inv1+creador1) | pre-review-catch | HVAC elbows are EXACT constant-radius arcs (centerline on x²+y²=R²), NOT splines. MEASURED: a spline sweep (rmf-frames/makeSweptTube) through a 90° corner drifts 0.093m = R·(√2−1) = 41%-of-R overshoot ("melted pipe") AND tears into a degenerate mesh; the torus-arc `hvac-fittings.elbow` holds x²+y²=R² to 5.5e-17. And three.js is NOT a CAD kernel: OCCT-WASM (48.9MB/7.1MB-min) fails the offline-single-file contract → optional online-only track, never a bundled dep. | [measured] full-chain PoC geom-verify readback; ocjs.org WASM sizes | staged |

## Active ledger

Two tables, BOTH Active, BOTH loaded at P0. Only the USER writes here — a run never edits an Active
row, not even to bump a status (`confirmed×N` is binding, so a bump is a run writing law).

Rows marked `promoted (fixed: …)` were already folded into a `references/` file and remain only as
tombstones — they are NOT re-applied as rules. Prune them at the next retro.

### Active — current schema (10 columns)

Promoted §Staged rows land HERE, keeping their provenance intact.

| date | project | design | track | pass | origin | how | learning | evidence | status |
|---|---|---|---|---|---|---|---|---|---|
| 2026-07-14 | threejs-hvac-prototipos | cinemex-hvac-lorawan | threejs | (tooling) | X1 closure, pre-P6 | pre-review-catch | An instrument that iterates "every X" must FAIL LOUD when it finds zero X — assert and report the input census; a "no differences" report over an empty set is a false green. | measured: 84-combo diff (DEFERRED-CORRECTIONS X1); first version resolved 0 presets and reported vacuous green | promoted (fixed: GATES.md §instrument corollaries, 2026-07-14 user-delegated) |
| 2026-07-14 | threejs-hvac-prototipos | cinemex-hvac-lorawan | threejs | surface/(spec) | X1 closure | mechanical-failure | When two instruments disagree about evidence geometry, MEASURE the gated artifact and re-express dependent thresholds as UNIT CONVERSIONS — never adopt either claim, never recalibrate. | measured: canvas 1.0818 vs test 0.8 vs ledger "4:3"; floors ×(636/900); 211/211 | promoted (fixed: GATES.md §instrument corollaries, 2026-07-14 user-delegated) |
| 2026-07-14 | threejs-hvac-prototipos | cinemex-hvac-lorawan | threejs | p7-delivery | P7 GLB export | wasted-retry | Preserve every P7 harness as a repo tool next to probe/capture/preflight — an unpreserved harness is a rebuild tax on the next design's P7. | research/tools/export-glb.mjs:5-7 (damper preserved nothing; cinemex rebuilt) | promoted (fixed: TRACK-THREEJS.md §Delivery kit, 2026-07-14 user-delegated) |
| 2026-07-14 | threejs-hvac-prototipos | cinemex-hvac-lorawan | threejs | p7-delivery | P7 GLB export | mechanical-failure | GLTFExporter throws on non-drawable textures (DataTexture/PMREM): strip those slots + environment/background, and import it through the PAGE's importmap so exporter and scene share one three.js. | research/tools/export-glb.mjs:44-68 | promoted (fixed: TRACK-THREEJS.md §Delivery kit caveats, 2026-07-14 user-delegated) |
| 2026-07-14 | threejs-hvac-prototipos | cinemex-hvac-lorawan | threejs | p6-final | p6 a1 capture | pre-review-catch | Updating the HUD pass-label is part of OPENING a pass (locked→active, before its first capture) — a dynamic-label rule with no owner moment ships stale labels into full-page sets. | measured: stale "Pase INTERACTION-UI" in P6's 31-shot set; pre-look caught it; one recapture (p6-final mechanical.json) | promoted (fixed: PIPELINE.md §Pass state machine, 2026-07-14 user-delegated) |
| 2026-07-14 | threejs-hvac-prototipos | cinemex-hvac-lorawan | threejs | (state) | interaction-ui gate close | mechanical-failure | Multi-shot gates COPY the representative capture to the exact canonical `<pass>[-l<L>]-attempt<N>.png` name — gate-state derives from that basename; suffix-only sets read as false failed(1)+drift. | measured: gate-state exit 1 → clean after the copy (2026-07-14) | promoted (fixed: GATES.md §Artifacts, 2026-07-14 user-delegated) |
| 2026-07-14 | threejs-hvac-prototipos | cinemex-hvac-lorawan | threejs | (orchestration) | L3 evidence chain, preflight→capture | mechanical-failure | A gating command may NEVER sit behind a pipe inside an `&&` chain — the chain gates on the PIPE's exit code, not the gate's. Run it standalone with FULL output to a log file and test `$?` (or `set -o pipefail`); tail the FILE, never truncate a gate tool's stream. | measured: `preflight 2>&1 \| tail -4 && capture` let a FAILED preflight ("Do NOT spend a gate attempt on this evidence chain") flow into a 46-min capture of dead evidence; 14 PNGs+sidecars deleted, capture needed SIGKILL; the same `tail` hid WHICH 2 checks failed | promoted (fixed: PIPELINE.md §Execution modes, 2026-07-15 unversioned edit — mtime observed 2026-07-16 (PIPELINE.md 00:07 on 07-15 vs the v1.5 SKILL.md bump 23:07 on 07-14), recorded then, no longer re-verifiable; traceability recorded 2026-07-16 user-delegated) |
| 2026-07-15 | threejs-hvac-prototipos | cinemex-hvac-lorawan | threejs | (tooling) | L4 GPU capture | pre-review-catch | QUALIFY a renderer before a renderer/driver switch produces gate evidence: pilot EVERY content class the evidence contract uses (canvas textures, sprites, DOM raster in `--page`, full DPR) and pixel-compare per class — a single-shot pilot qualifies nothing. | measured: `--gpu` adopted on a 0.61% single-shot delta (AA edges only), then rendered canvas-texture sprites + inactive DOM buttons as Chromium's failed-texture placeholder in its first DPR-3 `--page` set; pre-look caught it, set deleted, SwiftShader recapture, flag demoted to experimental-not-gate-safe in capture.mjs (its 4.5x speedup remains real) | promoted (fixed: GATES.md §Gate steps 2 Capture, 2026-07-16 user-delegated) |
| 2026-07-15 | threejs-hvac-prototipos | cinemex-hvac-lorawan | threejs | (tooling) | re-armed capture watchdog | mechanical-failure | A watchdog's liveness pattern must EXCLUDE the watchdog itself — match the interpreter+script (`pgrep -f "node.*capture.mjs"`) or exclude own PID. A watcher that can see itself reports motion in an empty room. | measured: the watchdog polled `pgrep -f "capture.mjs"`, a pattern its OWN command line contained → matched itself forever, never detected completion, timed out instead of reporting, then read as a phantom in-flight capture at the next status check | promoted (fixed: PIPELINE.md §Execution modes watchdog + GATES.md §instrument corollaries, 2026-07-16 user-delegated) |
| 2026-07-14 | threejs-hvac-prototipos | cinemex-hvac-lorawan | threejs | p6-final | p6 a1 vs p6 L2 a1 | mechanical-failure | Embed the judge output contract — `review.schema.json`'s EXACT field names — VERBATIM in every reviewer brief; never point a judge at the schema file. The judge returns that schema and nothing invented. | A/B measured in one round: p6-final-attempt1 + its predecessor returned off-schema JSON (invented layer keys, `evidence`/`expectation` fields) and were normalized BY HAND (mechanical.note); the L2 brief embedded the exact field names → p6-final-l2-attempt1 came back schema-clean on the first try | promoted (fixed: ROLES.md §render-reviewer, 2026-07-16 user-delegated) |
| 2026-07-14 | threejs-hvac-prototipos | cinemex-hvac-lorawan | threejs | (spec) | P6 L2, RTU amendment | improvised-win | After a mid-run spec refinement, P3 revalidation is SCOPED to the touched fields (post-refine-spec scoped P3) — the validator sees only what changed. A full re-sweep is cost with no signal. | measured: the RTU amendment's FAIL → prescribed-fix → PASS cycle took minutes because the validator saw only the touched fields (retro §Appended live 2026-07-14 night, item 4) | promoted (fixed: GATES.md §Verdict refine-spec + PIPELINE.md P3 row, 2026-07-16 user-delegated) |
| 2026-07-14 | threejs-hvac-prototipos | cinemex-hvac-lorawan | threejs | (orchestration) | P6 L2 correction round | improvised-win | ONE writer owns the working tree for a correction round; the orchestrator EXTENDS its scope with numbered extensions carrying pre-diagnosed root causes — never a second writer on the same tree. | measured: 11 items through one tree owner via numbered SendMessage extensions with pre-diagnosed root causes → 234/234 green (retro §Appended live 2026-07-14 night, item 5) | promoted (fixed: ROLES.md §Fan-out decision table, 2026-07-16 user-delegated) |
| 2026-07-16 | design3d-kit | (synthetic fixtures) | both | (tooling) | fixtures round, intentional RED test | pre-review-catch | A checker that resolves its threshold to null must FLAG, not skip — a PASS review with neither spec `quality_contract.global_min` nor `assumed_thresholds.global_min` is a contract violation (GATES.md §Retrofit gate mandates one of the two), never a silently skipped check. | [measured] the intentional RED test (globalmin-nomin fixture: verdict PASS at global_score 0.4 exited 0 "clean") + pre-fix assets/gate-state.mjs:124 `gmin ... ?? null` skip | promoted (fixed: assets/gate-state.mjs global_min contract flag, 2026-07-16 user-delegated) |
| 2026-07-16 | design3d-kit + anti-ai-ui-kit | (kit maintenance) | both | (kit hygiene) | 2026-07-16 session kit sweep | mechanical-failure | A kit that accepts a delta COMMITS it in the same session — accepted-but-uncommitted kit work was found in TWO kits the same session, and verify-kit-clean at SessionStart is the backstop that keeps it from recurring. | [measured] git status: design3d's unversioned 2026-07-15 PIPELINE.md edit (v1.6 changelog records the skipped traceability) + anti-ai-ui's 3 accepted deltas dirty since 07-15, landed as commit 02c8aef | promoted (fixed: assets/toolbelt/verify-kit-clean.sh both-kits SessionStart check, 2026-07-16 user-delegated) |
| 2026-07-18 | threejs-hvac-prototipos | cinemex-hvac-lorawan | threejs | (tooling) | client rounds 3-5 + rondas A/B + boot opt | user-correction | Give capture output an OWNER MOMENT: every capture is EVIDENCE (bound to a note/gate by exact canonical name or a documented glob) or EPHEMERAL (scratchpad, never `runs/assets/`); at round/pass close run a capture-GC that reports the census (kept/deleted/bytes) in the note and deletes orphans, protecting the canonical `<pass>[-l<L>]-attempt<N>.*` names gate-state derives from. Extends the 2026-07-12 on-gate-close deletion rule, which the measurement proved insufficient on its own. | [measured] 2026-07-18: runs/assets/ = 432 PNGs / 285 MB, only 28 bound by exact name; maintainer flagged the growth unprompted (retro §Appended live 2026-07-18) | promoted (fixed: GATES.md §Artifacts capture lifecycle + capture-GC, 2026-07-20 user-delegated; the 07-18 append skipped its at-append §Staged row per the queue-gap rule — folded directly on adjudication, traceability recorded here) |
| 2026-08-07 | threejs-hvac-prototipos | equipos-library | threejs | (gate/state) | equipos heavy /loop run | improvised-win | Give a flat multi-asset catalog run a spec-declared gate pass-subset (`gate_passes:`) — a simple stainless equipment asset needs only ONE materials gate, and the fixed 8-pass in-order ladder in `gate-state.mjs` can never derive green for it. | [measured] `gate-state.mjs` on lavavajillas & mesa-trabajo derives ALL passes `locked` (blockout has no PASS review → blocked at pass 1) though all 18 assets carry a materials PASS 0.76–0.80; run bypassed gate-state and used the materials review as done-marker; progress.yaml absent 18/18 | promoted (fixed: assets/gate-state.mjs gate_passes subset + PIPELINE.md §Triage flat-catalog + GATES.md §Verdict + DESIGNSPEC.md §Schema, 2026-08-07 tests green; complexity.tier auto-derivation deliberately NOT taken — subset is explicit-declare only) |
| 2026-08-07 | threejs-hvac-prototipos | equipos-library | threejs | materials | 16 stainless assets | improvised-win | Light headless BARE metal (metalness ~0.9) with `scene.environmentIntensity≈2.15` + a `HemisphereLight` + a FRONTAL `DirectionalLight` fill + roughness 0.30 brushed roughnessMap — otherwise it reflects the dark house horizon and reads black, worst on the vertical faces of boxy equipment (the house rig lights tops). | [measured] lavavajillas materials-attempt1 vertical body 0.73 dark → attempt2 PASS 0.78 after env2.15 + frontal fill (`materials-attempt2.review.json` feature `stainless-body-read`); recipe duplicated 16/18 HTMLs (`lavavajillas.html:77-104`) | promoted (fixed: TRACK-THREEJS.md §Generic defaults light-rig bullet, 2026-08-07; extracted to library/materials-textures/brushed-stainless-recipe.md) |
| 2026-08-07 | threejs-hvac-prototipos | equipos-library | threejs | blockout | mesa-trabajo capture | mechanical-failure | Default `OrbitControls.autoRotate = false` for gate captures — a rotating scene never settles, so `capture.mjs`'s frame-settle loop reports "frame never stopped changing" and writes `console_clean:false`. | [measured] `mesa-trabajo/runs/blockout-attempt1.console.json` `console_clean:false` issue "frame never stopped changing"; fixed in-run, final HTMLs set `autoRotate=false // OFF by default (gate captures need a settled frame)` | promoted (fixed: TRACK-THREEJS.md §Generic defaults OrbitControls line, 2026-08-07) |
| 2026-08-07 | threejs-hvac-prototipos | equipos-library | threejs | p7-delivery | equipos catalog run | pre-review-catch | Trigger library extraction PER ASSET in a heavy/catalog run — the heavy DAG runs per-asset P4/P5 then a single scene-assembly→P6→P7, so a catalog with NO scene never reaches P7 and extracts nothing. | [session-observed] 18 gate-passed reusables (brushed-stainless recipe, transmission-free glass, gooseneck spout) none extracted at run end; PIPELINE.md §Triage heavy had no per-asset P7 step | promoted (fixed: PIPELINE.md §Triage flat-catalog catalog extraction checkpoint, 2026-08-07) |
| 2026-07-16 | design3d-kit | (synthetic fixtures) | blender | (tooling) | fixtures round, blender-track test | pre-review-catch | Document what a Blender-track reviewer truthfully records for `mechanical.console_clean`/`budget_pass` — gate-state requires both `=== true` on EVERY PASS review, but the Blender track has no browser console or probe, so today a truthful reviewer cannot satisfy the checker without inventing values. | [measured] first synthetic exercise of the blender ladder: tests/gate-state.test.mjs blender-track scenario (its fixture had to hardcode console_clean/budget_pass true to derive clean), 2026-07-16 | new |
| 2026-07-22 | threejs-hvac-prototipos | compresor-tornillo-aire | threejs | lighting-camera | lighting l1 | mechanical-failure | RectAreaLight + RectAreaLightUniformsLib stall the SwiftShader shader-compile (probe returns 0 draws) and make the render heavy (capture timeout at DPR 2-3) — in the headless threejs track use DirectionalLight/SpotLight, never RectAreaLight. | [measured] preflight FAIL "probe reports 0 draws" + hero DPR3/train DPR2 exit 124; fixed by a shadowless DirectionalLight softbox (disenos/compresor-tornillo-aire/runs) | new |
| 2026-07-22 | threejs-hvac-prototipos | compresor-tornillo-aire | threejs | materials l1 | blind-review-defect | Calibrate paint albedo by the VALUE the render shows after ACESFilmic tonemapping, not by the nominal hex — RAL 7035 #c9cdce renders near-white, drop to #54585c to read graphite grey. | [measured] canopy 0.70 FAIL (near-white) -> albedo #54585c PASS 0.82 | new |
| 2026-07-22 | threejs-hvac-prototipos | compresor-tornillo-aire | threejs | blockout l1 | improvised-win | Per-pass threshold for fine-detail critical features: in a voxel blockout (0.1 m) the fine identity (airend flanges/sight-glass) does not fit — use blockout_threshold (0.72, massing) and the final threshold (0.78) from structural onward. | [measured] airend failed(3) 0.50-0.58@0.78 in blockout -> 0.73@0.72 after calibration, 0.79-0.83@0.78 structural+ | new |
| 2026-07-22 | threejs-hvac-prototipos | compresor-tornillo-aire | threejs | (evidence) | blockout/structural gates | pre-review-catch | Coverage: an occluded critical feature (aftercooler-fan on the -X face) needs its own camera angle via a query param; verify it in the preflight coverage check BEFORE spending the gate (it failed twice on framing, not absence). | [measured] aftercooler-fan 0.42/0.58 until ?view=cooler (az225) added -> 0.81 | new |
| 2026-07-22 | threejs-hvac-prototipos | compresor-tornillo-aire | threejs | p7-delivery | GLB export | mechanical-failure | export-glb.mjs requires `<html data-app-ready="true">` + hook `globalThis.<name>.runtime.scene`; without the flag waitForSelector hangs and the frame detaches. Document in TRACK-THREEJS §Delivery. | [measured] export "frame got detached" -> after data-app-ready + hook __airtechApp: glb 866KB glTF v2 valid | new |
| 2026-07-22 | threejs-hvac-prototipos | compresor-tornillo-aire | threejs | (state) | gate-state naming | mechanical-failure | The review/screenshot filename must use the EXACT pass name from the ladder (lighting-camera, not lighting) or gate-state derives 'locked' + drift. | [measured] drift lighting-camera cache=passed derived=locked -> renamed to lighting-camera-attempt1.* | new |
| 2026-08-07 | threejs-hvac-prototipos | equipos-library | threejs | materials | laminadora capture | mechanical-failure | Retry a threejs gate capture ONCE on a network-failed sidecar (no canvas) before treating it as a spent attempt — the unpkg three.js import transiently fails with `net::ERR_QUIC_PROTOCOL_ERROR`; consider vendoring three.js to drop the network dependency from gate evidence. | [session-observed] laminadora `materials-attempt1.review.json` defect: "first capture failed on a transient unpkg QUIC network error (no canvas); re-captured clean" (failing sidecar overwritten, current console.json clean) · re-observed 2026-08-09 (unpkg QUIC hit the showcase + filtrado-alberca captures; retry succeeded both times) | confirmed×1 |
| 2026-08-07 | threejs-hvac-prototipos | nave-panccadia/equipos | threejs | (tooling) | capture-gc first run | improvised-win | Dedup the `<slug>.png` <-> passing-attempt `<pass>-attempt<N>.png` BYTE-TWIN in the flat catalog. gate-state.mjs:125 derives PASS from the attempt basename's `.png` existing, and the flat-catalog `<slug>.png` is a byte-identical COPY of it — so every shipped asset carries the same PNG twice (~5MB across 18 nave-panccadia assets). To prune the attempt twin safely, first teach gate-state to accept the canonical `<slug>.{png,review.json}` as a valid pass witness (contract change + test), then let capture-gc drop the attempt twin. | [measured] md5(bascula-piso.png)==md5(materials-attempt1.png); md5(freidora.png)==md5(materials-attempt2.png) 2026-08-07; gate-state.mjs:123-131 requires `r.base + '.png'` for the PASS to count | promoted (fixed 2026-08-07 user-delegated: gate-state.mjs `hasPngWitness` canonical fallback + `canonical-witness` test; capture-gc.mjs `--dedup`; applied on nave-panccadia = 7.39MB freed / 18 twins, 18/18 gate-clean) |
| 2026-08-07 | threejs-hvac-prototipos | nave-panccadia/equipos | threejs | (state) | 9 assets missing progress.yaml | mechanical-failure | No tool wrote runs/progress.yaml — it was hand-authored per run, so an original build that skipped it leaves gate-state reporting a permanent cache-missing drift. Add `gate-state.mjs --write-cache`: emit the cache from the derived ladder, gated on zero semantic problems (never cache a broken state), then self-verify. | [measured] 9 nave-panccadia assets (bascula/batidora/cuentalitros/estanteria/fermentadora/freidora/horno-domino/horno-rotativo/silla-lavabo) had no progress.yaml → --write-cache wrote + clean exit 0 on all 9, 2026-08-07 | promoted (fixed 2026-08-07 user-delegated: gate-state.mjs --write-cache) |
| 2026-08-09 | threejs-hvac-prototipos | jaula-seguridad | threejs | materials | pre-review orchestrator look | pre-review-catch | Project the subject's world bbox to NDC and check the framing BEFORE capturing: require the corners inside ±0.93 and the top below +0.75 so the HUD band stays clear. No mechanical check scores where the subject lands in the frame. | [measured] two framings green on probe + sidecar while visibly broken: rack-drive-in ran the top bracing through the 4-line HUD, jaula-seguridad put the base plates on the bottom edge at ndcY −0.981 | promoted (fixed: 2026-08-09 numerical-methods session — geom-verify.mjs checkFraming = bbox->NDC with a w<=0 guard + HUD-overlap + fullyVisible/cropped, wired via framing-probe.mjs; kit PRs #1/#3) |
| 2026-08-09 | threejs-hvac-prototipos | jaula-seguridad | threejs | materials | colorTarget authoring | wasted-retry | Anchor a `colorTarget` crop by projecting the part's world position to pixels, then confirm two independent instances agree — never by scanning the render for a hue. A low standard deviation proves the crop does not straddle an edge; it proves nothing about identity. | [measured] a hue scan for "safety yellow" returned 328 windows at deviation 0.0 that were all WOODEN PALLET ([162,126,77] vs wood #a8814f, same B/R); the real guard measured [199,162,41] | new |
| 2026-08-09 | threejs-hvac-prototipos | jaula-seguridad | threejs | (tooling) | first gate in a linked worktree | mechanical-failure | A linked git worktree has no `node_modules`, so the entire `research/tools/` harness dies with `ERR_MODULE_NOT_FOUND` on `puppeteer-core`. Symlink the main checkout's `node_modules` AND exclude it via `.git/info/exclude` — `.gitignore` lists `node_modules/` with a trailing slash, which does not match a symlink. | [measured] `preflight.mjs` exit 1 "Cannot find package 'puppeteer-core'"; after symlink, preflight PASS 26 draws / 14322 tris; `git status` showed `?? node_modules` until the exclude was added | new |
| 2026-08-09 | threejs-hvac-prototipos | jaula-seguridad | threejs | materials | padlock, native crop | pre-review-catch | The native-resolution crop cuts BOTH ways: the kit prescribes it so the judge does not score a defect the downscale invented, but it also reveals defects the downscale HIDES. A small bare-metal part read as a plausible pale mark at frame scale and as a flat black silhouette at 1:1. | [measured] /tmp/lock-native.png (black) → practical placed on the solved reflection direction R = 2(N·V)N − V → /tmp/lock-native3.png (body, shackle arc, keyway legible); metalness held at 0.95 throughout | new |
| 2026-08-09 | threejs-hvac-prototipos | jaula-seguridad | threejs | materials | inline reviewer | user-correction | When the session cannot spawn a fresh-context reviewer, do NOT present the resulting verdict as contract-passed: record the self-review explicitly in `mechanical.note` and flag it to the user. GATES.md permits an inline role hat only in quick mode. | [session-observed] `materials-attempt1.review.json` mechanical.note records the inline reviewer as deviation (1) of three | new |
| 2026-08-09 | threejs-hvac-prototipos | catalog shell-template (family-wide) | threejs | materials/lighting | consolidated-by-session-A: env recipe inert in r160 | mechanical-failure | `scene.environmentIntensity` does NOT exist until three r163; the catalog pins three@0.160.0, so `shell-template.html`'s `scene.environmentIntensity` is a SILENT no-op (JS accepts the field, the renderer never reads it). The ACTIVE brushed-stainless recipe (`environmentIntensity ≈ 2.15`) therefore NEVER applied on the threejs catalog track — assets light via the explicit Hemisphere+Directional rig, not IBL. 65 catalog specs document this dead deviation in their `lighting:` section as if applied (a FALSE spec claim). The live knob in r160 is per-material `material.envMapIntensity` (+ `scene.environment`/PMREM). Do NOT reactivate without recalibrating 100+ assets (it invalidates every measured colorTarget). | [measured] THREE independent sessions (skid-hidroneumatico, bomba-vertical-pozo, empacadora-flowwrap): `revision "160"`; `environmentIntensity` = 0 occurrences in the r160 bundle vs `envMapIntensity` = 8; the field reads `esPropiedadDeclarada:false, esPropiaDelObjeto:true` | promoted (fixed: 2026-08-09 — kit PR #2 corrected the stainless recipes to material.envMapIntensity + AgX; documented in research blocks B125/B126. NOTE: this row says "until r163"; B126 source-verified it as r164+ — either way inert in r160) |
| 2026-08-09 | threejs-hvac-prototipos | porton-corredizo / skid-cip | threejs | materials | consolidated-by-session-A: NDC framing green-but-wrong | pre-review-catch | The NDC framing check can pass GREEN while measuring the wrong thing: maximizing the bbox RECTANGLE rewards a pronounced DIAGONAL (a long thin subject seen end-on has a huge bbox but a tiny real footprint). When the subject aspect ratio is far from the frame, constrain azimuth to near-perpendicular + require an explicitly CENTERED result, accept honest occupancy. AND call `controls.update()` BEFORE projecting — OrbitControls re-aims to target (0,0,0) in its constructor, so projecting without it measures a camera that is NOT the render camera. | [measured] porton passed at 61% occupancy reading as a fence to the horizon; skid-cip top +0.989 false vs +0.572 real until controls.update() | promoted (fixed: 2026-08-09 — geom-verify framingMetrics requires centered + fullyVisible and accepts wide/thin subjects via a one-axis span, kit PR #10; framing-probe reads the live render camera via the __qaFraming hook so the controls.update timing is moot) |
| 2026-08-09 | threejs-hvac-prototipos | porton-corredizo / esclusa-personal | threejs | (state) | consolidated-by-session-A: blind reviewer cold-loads | pre-review-catch | An asset with an animation channel must START in the state, not reach it by click: a blind reviewer only cold-loads the page by URL, so a click-only state is never seen. If the critical rule reads only in that state, expose it via `?param` or as the default state. Also: which SIDE the camera is on is mechanical-blind — ask "what is between the eye and the subject" (a fence occluding the counterweight); moving the camera means moving the key+fill or bare metal goes black. | [session-observed] the 50% counterweight rule (porton) reads only in the abierto state | new |
| 2026-08-09 | threejs-hvac-prototipos | esclusa-personal / empacadora-flowwrap | threejs | (behavior) | consolidated-by-session-A: prove behavior by actioning | pre-review-catch | If behavior defines the asset (an enforced interlock, a sequence), the EVIDENCE must be behavioral: action the sequence (open door B with A open) with a `console.error` that fires on violation and confirm `errors:[]`, do NOT read the handler. The gate reads `console.error` (and exceptions), NOT `console.assert` — a failed assert is silent (exit 0). And a linked worktree has no node_modules → the harness dies ERR_MODULE_NOT_FOUND on puppeteer-core (symlink + .git/info/exclude). | [measured] empacadora `console.assert(envelope_ok)` failed silently → 3 packages floating, exit 0; esclusa interlock proven by driving both doors | new |
| 2026-08-09 | threejs-hvac-prototipos | suavizador / skid-hidro / molino | threejs | (geometry) | consolidated-by-session-A: substituting cut must not net-remove | reviewer-defect | A CORTE that SUBSTITUTES geometry has two distinct lists (hidden vs restored); if the substitute lacks a piece, what rested on it FLOATS — gate-blind, counts green. Concentric envelopes: cutting only the outer shell shows the inner bladder and reads solid. Cheap criterion = CONTINUITY: the visible body must TOUCH its neighbor (gap ≤ 0) in BOTH states; the contained medium (water/load) is always-present WITHOUT a pair, so the cut only OPENS and never CREATES geometry. | [measured] suavizador gapBajoCuello 0.117→-0.035; molino negative-height discharge (fondo−bancada same value); skid-hidro casco+vejiga 12 disjoint uuids | promoted (fixed: 2026-08-09 — geom-verify checkGeometry (concentric containsBox + high-IoU AABB) + verticalGap give the gate-blind concentric/floating mechanical check this asked for, advisory; kit PR #1) |
| 2026-08-09 | threejs-hvac-prototipos | catalog QA tools (family-wide) | threejs | (tooling) | consolidated-by-session-A: BASE vs BASE_URL measures wrong tree | mechanical-failure | The catalog QA tools read DIFFERENT env names (gate `verify-catalog-asset`=`BASE_URL`; `probe-state`/`hole-probe`/`audit-asset`=`BASE`), and the 8899 default serves ANOTHER worktree under heavy concurrency — passing the wrong name silently inherits 8899 and measures the wrong tree with exit 0 (green over another session's tree). Fix: accept `BASE_URL||BASE` in all four AND PRINT the resolved BASE before measuring; use an OWN free port + `curl` a worktree-only path (verify identity in two directions: your asset 200, an alien path 404). | [measured] a gate reported the right `__slugApp` only after `BASE=8127`; 8899/8123 served catalog-session-b's tree (404 for the own asset) | new |
| 2026-08-15 | threejs-hvac-prototipos | (kit) | threejs | (tooling/delivery) | user directive 2026-08-15 | user-correction | STRUCTURAL DELTA — full proposal in `disenos/catalog/EXTERNAL-ASSETS.md §A`: make OFFLINE-FIRST the `shell-template.html` default — vendor three.js locally (`vendor/three/` + a local importmap), never a run-time CDN; adopt `build-offline.mjs`/`verify-offline.mjs` from `datacenter-hotspot-sinCDN`. Re-confirms the §Active unpkg-QUIC rows that already recommended vendoring. | [measured] working precedent `disenos/datacenter-hotspot-sinCDN/` (`vendor/three/` + local importmap + `verify-offline.mjs`); `nave-3sistemas` still pinned to `unpkg.com/three@0.160` | new |
| 2026-08-15 | threejs-hvac-prototipos | (kit) | threejs | (new track) | user directive 2026-08-15 | user-correction | STRUCTURAL DELTA — full proposal in `EXTERNAL-ASSETS.md §B`: add a THIRD track `external-mesh` beside procedural-direct/needs-research — source CC0/CC-BY GLBs, normalize to 1u=1m + embed textures + self-contained, gate with a NEW `verify-external-asset.mjs` (license+provenance+load+no-external-URIs+scale+non-black), NEVER the ΔE00 gate; an external mesh is never a `[CERT]` measured subject. | [session-observed] user authorization 2026-08-15 after tradeoff review; license feasibility verified (CC0/CC-BY redistributable; NC / Sketchfab-standard rejected) | new |
| 2026-08-15 | threejs-hvac-prototipos | nave-3sistemas | threejs | lighting-camera | field reconciliation (vibe-coding 3D forum) + user directive | improvised-win | Adopt a hideable master dev-UI (lil-gui) exposing camera modes / lights / materials with a JSON EXPORT of the tuned settings, hotkey-gated and shipped disabled — turns the lighting-camera corrections into dials the operator turns instead of blind guesses. | [inferred] maps directly onto `nave-3sistemas/runs/DEFERRED-CORRECTIONS.md` DC1–DC3 (skid fill, reframe, luminaire-plan); pattern from a community thread reviewed 2026-08-15 (`EXTERNAL-ASSETS.md §E`) | new |
| 2026-08-15 | threejs-hvac-prototipos | nave-3sistemas | threejs | lighting-camera | attempt1→2 gate | blind-review-defect | After ANY geometry/material change, RE-GATE THE WHOLE evidence set, never just the changed feature — a fresh judge re-scores untouched features too and a marginal PASS can flip. | [measured] luminaire-grid PASS 0.76 (`lighting-camera-attempt1.review.json`) → FAIL 0.55 (attempt2) with the luminaire geometry UNCHANGED between the two; only the judge and the surrounding detail changed | new |
| 2026-08-15 | threejs-hvac-prototipos | nave-3sistemas | threejs | p6-blind | attempts 1-5 | improvised-win | Escalate the FINAL verdict to a 2-judge PANEL when a feature has BOUNCED across attempts, not only after N same-feature failures — single-judge variance at the margin is real and a panel stabilises it. | [measured] luminaire-grid swung 0.76↔0.55 and ahu-and-duct-run 0.76↔0.71 across single judges; the 2-judge panel returned a stable unanimous 0.79 (`lighting-camera-panel.review.json`) | new |
| 2026-08-15 | threejs-hvac-prototipos | nave-3sistemas | threejs | (geometry) | attempt4→5 gate | blind-review-defect | Differentiate co-located services by CROSS-SECTION / shape, not material alone — two round cylinders read as ONE system even with different materials and markers. | [measured] ahu-and-duct-run FAILed 0.71 with a round HVAC trunk beside the round compressed-air pipe; PASSed under both panel judges once the trunk became RECTANGULAR (commit 191674b) — material (galvanized vs stainless) had not separated them | new |
| 2026-08-16 | threejs-hvac-prototipos | nave-panccadia/equipos | threejs | (reference-intake) | condensadora-vertical + estanteria-alambre P1 | mechanical-failure | Read source video frames at NATIVE RESOLUTION during reference intake — a contact-sheet thumbnail (~300 px) misled on material and colour three times in one run: chrome wire read as "matte white", a window frame as "black posts", a sage-green cabinet as "beige". Use `research/tools/video-inspect.mjs` (new this run) or equivalent to extract and view hi-res individual frames before authoring material calls. | [measured] condensadora-vertical spec authored "beige" (#a8a08c) from contact sheet; native-res read corrected to sage grey-green; dE00 0.91 confirmed. estanteria-alambre spec initially authored metalness 0.9 (chrome wire); native-res read changed it to metalness 0.0 (white epoxy) — the identity-defining call (dE00 5.64 gate PASS). INTEGRATION-2026-08-15.md §C stale hypothesis: third miss (tall industrial / black posts) also from contact sheet. | promoted (fixed: references/DESIGNSPEC.md §Reference-image intake, 2026-08-16 user-authorized) |
| 2026-08-16 | threejs-hvac-prototipos | nave-panccadia/equipos | threejs | materials | condensadora-vertical + estanteria-alambre + racking-pallets spec authoring | pre-review-catch | When a source photo or video exists, anchor `colorTarget.srgb` to a geometry-anchored crop of THAT SOURCE — never of your own render. A render-derived target makes the gate tautological (certifies the render matches itself, not the subject). The kit's "authored from a golden render when no photo exists" is a fallback for when the source is genuinely unavailable, not a default; when used as fallback, label it explicitly as a REGRESSION DETECTOR in both the spec and the review. | [measured] condensadora-vertical: video-anchored target (166,170,153); dE00 0.91 PASS. estanteria-alambre: video-anchored (214,214,203); dE00 5.64 PASS. racking-pallets: field video unusable (shadow, lum ~50 vs ~108 lit); reference render inside sealed freezer (no cutaway); render-derived target (59,111,227); spec and review both label it regression-detector explicitly. | promoted (fixed: references/DESIGNSPEC.md §Schema · references/GATES.md §Objective material-colour gate, 2026-08-16 user-authorized) |
| 2026-08-16 | threejs-hvac-prototipos | nave-panccadia/equipos | threejs | (spec) | racking-pallets spec authoring, builder catch | mechanical-failure | `gate_passes:` MUST be a TOP-LEVEL key in `design-spec.yaml` — if nested under `quality_contract:` or any other parent, `gate-state.mjs` cannot see it and derives the full 8-pass ladder with all passes `locked`, so the asset never closes green. DESIGNSPEC.md §Schema lists it in the Optional block at the same level as `quality_contract`, but a first-time author naturally groups it there. | [measured] racking-pallets initial draft nested `gate_passes:` under `quality_contract:`; builder caught before gate. All three new specs carry `# flat-catalog subset (TOP-LEVEL per DESIGNSPEC.md §Schema) so gate-state.mjs derives the subset (if nested under quality_contract it is invisible and the asset never closes green)` as in-spec guards. gate-state.mjs reads `spec.gate_passes` at the top level. | promoted (fixed: references/DESIGNSPEC.md §Schema, 2026-08-16 user-authorized) |
| 2026-08-16 | threejs-hvac-prototipos | nave-panccadia/planta-frigorifica | threejs | (integration/capture) | v24 scene integration, cold-room shelving and cond-support views | blind-review-defect | The framing-probe's `fullyVisible` flag checks whether the subject's AABB corners project inside the NDC viewport — it cannot detect physical occlusion by non-subject geometry. A capture can come back a blank wall while `framing: ok` (subject projects inside frame, but a host wall sits between camera and subject). In scene-integration captures, extend `VIEW_HIDE` to include EVERY opaque layer between the camera and the subject, not just the subject's own label sprites. | [measured] scene-gate.review.json: cond-support.png "mis-framed: blank wall with a tiny fragment, subject out of frame. The dedicated evidence is unusable." The shelving capture needed VIEW_HIDE extended with cold/walls/spaces/doors layers. framing-probe reads subject NDC projection (ok) but not what blocks the camera-to-subject ray (the host wall). | promoted (fixed: references/GATES.md §Mechanical checks, 2026-08-16 user-authorized) |
| 2026-08-16 | threejs-hvac-prototipos | nave-panccadia/planta-frigorifica | threejs | (integration) | v24 scene integration, MOBILIARIO pre-check | pre-review-catch | Before placing a new master into a GENERATED scene, check whether the generator's CAD layer or furniture dictionary already carries a placeholder for the same object. A CAD drawing can carry furniture footprints for new equipment; placing the new master on top renders the object twice while console and count checks read clean. Guard with a geometric filter (project furniture footprints against the new asset's footprint + lateral offset) that FAILS LOUD if it suppresses zero matches — zero matches means the guard is not measuring anything. | [measured] INTEGRATION-2026-08-15.md §C: CAD MOBILIARIO boxes for racks; builder applied a geometric filter; v24 pallet-racking-runs score 0.78 PASS. Fail-loud-zero echoes GATES.md §Instrument corollaries. | promoted (fixed: references/PIPELINE.md §Triage, 2026-08-16 user-authorized) |
| 2026-08-16 | threejs-hvac-prototipos | nave-panccadia/equipos/estanteria-alambre | threejs | structural | estanteria-alambre structural, pre-gate builder catch | pre-review-catch | When a shelf or beam spans to a SLOTTED-ANGLE (L-section) upright, span to the SHEET FACE of the nearest leg — not to the L's bounding box. The open quadrant of the L is inside the bbox but touches no sheet: a span measured to "post width" can float 20–35 mm clear of the metal. Assert overlap DEPTH at both ends, measured from the sheet face. | [session-observed] estanteria-alambre structural review defects[1]: "Builder disclosed and fixed a pre-capture floating-shelf defect (shelf edge fell in the open quadrant of the L-post, 33 mm gap at 4 corners × 4 levels); the render shows shelves seated on the posts. Fix taken on the builder's per-instance measurement (session-observed), not independently re-measured from geometry." | promoted (fixed: references/DESIGNSPEC.md §Authoring rules, 2026-08-16 user-authorized) |
| 2026-08-16 | threejs-hvac-prototipos | nave-panccadia/planta-frigorifica | threejs | (integration/materials) | v24 scene integration, cold-room shelving read | blind-review-defect | Integrating a well-calibrated master into a scene with a different light rig can change how metals read. Measure whether dark-metal is SYSTEMIC (compare new post lum against an existing metal in the same scene) or ASSET-SPECIFIC. A systemic problem requires a GLOBAL rig change (IBL/exposure) that repaints all metals and requires re-gating the whole scene — surface it to the user as a scene-level decision; never apply a per-asset brightness patch to a scene-level rig problem. | [measured] scene-gate.review.json coldroom-wire-shelving note: "post lum 40 vs an existing viewer steel column lum 28 — systemic, not a port defect. Resolving it means a global rig change that repaints every metal in the viewer." Decision deferred as user choice in v24. | promoted (fixed: references/TRACK-THREEJS.md §Generic defaults, 2026-08-16 user-authorized) |
| 2026-08-16 | threejs-hvac-prototipos | nave-panccadia/equipos/racking-pallets | threejs | (process) | racking-pallets framing as "improve the catalog asset" | pre-review-catch | The "improve the catalog asset" or "reuse an existing master" framing is a specific bypass vector for the master-first discipline — it leads the orchestrator to nearly skip design→gate in favour of direct integration. Guard: if the deliverable is a new or substantially reworked object in the scene, run the full design→gate cycle regardless of how many existing assets it resembles. Re-confirms §Staged condensadora lesson 2026-08-10. | [session-observed] INTEGRATION-2026-08-15.md §Work items C: "racking was nearly built straight into the scene generator because it was framed as 'improve the catalog asset'." racking-pallets/runs/ shows structural PASS 0.85 + materials PASS 0.85 before integration — the discipline held because the orchestrator caught the framing trap. | new |
| 2026-08-16 | threejs-hvac-prototipos | nave-panccadia/equipos/racking-pallets | threejs | optimization | racking-pallets build, 162 box draws → 4 after merge | improvised-win | When merging geometry by material to reduce draw calls, keep each TOGGLEABLE sub-group in its OWN merge pass — a merge that absorbs a toggleable group destroys the toggle's geometry (the button hides a group with no distinct geometry left). Build one merge per material for static members, then a separate merge per material for each toggleable sub-group. | [measured] racking-pallets structural review defects[1]: "Builder merged box-per-draw geometry 162 → 4 draws, keeping decks in their own merge for the DECKS toggle." structural-attempt1-nodecks.png confirms the toggle works after the merge. 4 draws / 1290 tris vs 80/250k budget. | promoted (fixed: references/TRACK-THREEJS.md §Pass ladder, 2026-08-16 user-authorized) |
| 2026-08-21 | threejs-hvac-prototipos | COB-IM2 | threejs | (intake) | COB-IM2 DWG→3D + 3 user-supplied CV reconstruction sessions | retro-synthesis | Reconstructing from a CAD file has two routes; ALWAYS prefer reading real entities (DWG→DXF via a reader ladder: system libredwg → ODA → compile-from-source → ezdxf) over CV-on-raster. CV invents scale, elevation, semantics AND co-registration — a SIFT similarity between sheets reports a spurious scale/rotation the CAD does not contain (raster px/m, not geometry). Route 2 (CV) is a last resort only when no reader runs AND a real-resolution raster exists; the ~256×115 embedded DWG thumbnail does NOT qualify. | [CERT-live] dwg2dxf == dwgread direct == ODA File Converter on the modelspace payload (COB-IM2 corpus B12); 14A embedded thumbnail measured 256×115 via dwgbmp; B4 co-registers the 3 sheets by pure translation (content-verified) while a CV session claimed scale 0.917 | promoted (fixed: references/PIPELINE.md §Triage CAD→3D intake, 2026-08-21 user-directed) |
| 2026-08-21 | threejs-hvac-prototipos | COB-IM2 | threejs | (architecture) | COB-IM2 vanilla viewer + a user-supplied R3F Phase-2 build | retro-synthesis | For a CAD→3D deliverable that becomes a dashboard, plan TWO phases and DECOUPLE the certified JSON data layer from the render layer: Phase 1 = vanilla three.js offline single-file (reconstruction; offline is a dev constraint, not the product), Phase 2 = React + R3F online (product). R3F wraps three.js so Phase-1 geometry reuses via `<primitive>` and Phase 1 is never throwaway; JSON goes by prop/API. | [session-observed] the user-supplied R3F `DuctViewer.jsx` takes JSON by prop and reuses the geometry; the vanilla Phase-1 viewer is QA-verified in `disenos/COB-IM2/`. Migration cost is REASONED (R3F wraps three), not yet measured on a real port | promoted (fixed: references/TRACK-THREEJS.md §Generic defaults, 2026-08-21 user-directed) |
| 2026-08-21 | threejs-hvac-prototipos | COB-IM2 | threejs | (delivery) | user-supplied R3F Phase-2 session | retro-synthesis | R3F track gotchas: per-frame animation in `useFrame` on the three objects (`damp` + `userData`), NEVER React state; drei `<Html>` for labels not canvas sprites; Next.js needs `dynamic(ssr:false)` + an explicit parent height or the canvas silently collapses to blank; an R3F component CANNOT preview as a Claude artifact (CSP blocks esm.sh) — ship a browser-only esm.sh + Babel HTML preview. | [session-observed] user-supplied R3F session; the Claude artifact CSP allows only self-contained assets + Google Fonts, so esm.sh is blocked (tool-doc known) | promoted (fixed: references/TRACK-THREEJS.md §Generic defaults, 2026-08-21 user-directed) |

### Active — legacy schema (7 columns, pre-provenance)

Already human-reviewed history. Do NOT back-fill provenance into these; do NOT add new rows here.

| date | design | track | pass | learning | evidence | status |
|---|---|---|---|---|---|---|
| 2026-07-12 | chiller-tornillo-agua | threejs | smoke/gate | Verify console-clean from the `<basename>.console.json` sidecar capture.mjs writes — probe/capture success alone never proves console state. | disenos/chiller/runs/smoke-2026-07-12/review.json (mechanical.note) | promoted (fixed: research/tools/capture.mjs + GATES.md §Gate steps) |
| 2026-07-12 | chiller-tornillo-agua | threejs | smoke/gate | Treat probe.mjs `fps` as informational only under SwiftShader (returned 0 with 9 frames); gate on draws/tris. | smoke run probe output | promoted (fixed: GATES.md §Gate steps + TRACK-THREEJS.md §QA commands) |
| 2026-07-12 | chiller-tornillo-agua | threejs | smoke/gate | Gating a spec-less legacy design needs a minimal design-spec.yaml first, or explicitly recorded assumed thresholds (global 0.75 / feature 0.70) in the review JSON. | smoke run had no spec; thresholds were assumed | promoted (fixed: GATES.md §Retrofit gate + PIPELINE.md §Triage quick) |
| 2026-07-12 | chiller-tornillo-agua | threejs | smoke/gate | Serve the REPO ROOT on :8123 and run node FROM the repo root with repo-root-relative paths — probe/capture resolve against the server root. | smoke run resolved the ambiguity by trial | promoted (fixed: TRACK-THREEJS.md §QA commands) |
| 2026-07-12 | chiller-tornillo-agua | threejs | smoke/gate | python3 `jsonschema` is absent in this env — validate review JSON with the node structural fallback one-liner. | smoke run validation step | promoted (fixed: GATES.md §Review validation) |
| 2026-07-12 | chiller-tornillo-agua | threejs | smoke/gate | capture.mjs names outputs after the html basename — copy/rename to `<pass>-attempt<N>.*` gate names before recording the gate. | smoke run artifact naming clash | promoted (fixed: GATES.md §Artifacts + TRACK-THREEJS.md §QA commands) |
| 2026-07-12 | (env) | blender | bootstrap | This machine runs WSL2 mirrored networking: official Lab MCP defaults (localhost:9876) reach Windows Blender directly — skip the NAT/interop workaround; server installed at ~/.local/share/blender-mcp-venv/bin/blender-mcp, registered user-scope. | live send_code test returned Blender 5.1.1 ok | confirmed×1 |
| 2026-07-12 | (policy) | both | gate | On gate close, DELETE superseded capture PNGs (failed attempts) and raw pre-rename capture outputs; keep ONLY the passing attempt's screenshot (required for state derivation) + every review JSON. User mandate: captures must not accumulate garbage. | user instruction 2026-07-12 | promoted (fixed: GATES.md §Artifacts) |
| 2026-07-12 | damper-motorizado | threejs | materials/gate | probe.mjs can report draws/tris = 0 when SwiftShader shader-compile (e.g. MeshPhysicalMaterial clearcoat) stalls past the sampling window — retry once before treating 0 as a broken scene; the capture PNG is the truth check. | materials gate: first probe 0/0, second 123/8158, capture rendered fine | promoted (fixed: TRACK-THREEJS.md §QA commands) |
| 2026-07-12 | damper-motorizado | both | gate | Modeler self-verification (scene-graph facts, luminance measurements) can contradict the SAVED render — corrections must be verified on the saved capture crop; the render seen by the blind reviewer is the only acceptance authority. | Blender blockout att.1 (builder sanity PASS vs judge 0.60 FAIL) + lighting att.1 (claimed 38→47 L vs render still crushed) | promoted (fixed: GATES.md §Blind-review protocol) |
| 2026-07-12 | damper-motorizado | threejs | blockout | Include `<link rel="icon" href="data:,">` in every generated HTML — the browser's automatic favicon 404 pollutes the console sidecar (console path, not network path). | blockout gate attempt 1 console error | promoted (fixed: TRACK-THREEJS.md §Generic defaults) |
| 2026-07-12 | damper-motorizado | threejs | interaction | capture.mjs cannot pass query strings — interaction demo states (?demo=hotspot) need a redirect shim until the tool grows a --url-suffix flag (delta proposed). | interaction gate .demo.html shim | promoted (fixed: research/tools/capture.mjs --url-suffix + TRACK-THREEJS.md §QA commands) |
| 2026-07-12 | damper-motorizado | threejs | lighting | Keep the HUD pass-label dynamic — a stale subtitle reads as evidence-hygiene defect to blind judges. | lighting att.1 defect #6 | promoted (fixed: TRACK-THREEJS.md §Generic defaults) |
| 2026-07-12 | damper-motorizado | both | state | progress.yaml cache corrupted silently during run-1 (indentation nesting bug); gate-state.mjs now validates cache vs derivation at resume/gate close | council #1 finding | promoted (fixed: assets/gate-state.mjs + PIPELINE.md) |
| 2026-07-12 | damper-motorizado | threejs | evidence | Gate evidence must be produced ONLY by the canonical harness (capture.mjs --url-suffix) — temp per-pass scripts create sidecar shape drift (extra fields) that breaks provenance; the 4R reliability lens caught one. | optimization demo sidecar "query" field | promoted (fixed: --url-suffix exists; rule here) |
