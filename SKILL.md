---
name: design3d
description: "Trigger: /design3d, 'create/design a 3D model|equipment|scene', 'voxel/realistic pass', Blender modeling. Dual-track spec-first pipeline with gated passes."
user-invocable: true
license: Apache-2.0
metadata:
  author: cristian-angeles
  version: "1.19"
  changelog: "1.19 (2026-08-26, user-directed AUTONOMOUS multi-session continuation of v1.18 — 6 sessions in per-lane self-paced loops, each delta built in its own worktree, PR'd to master with INDEX+library-header hygiene, peer-reviewed, integrator-merged; all library tests green; 11 deltas, 32 library modules): NET-NEW SPATIAL-ENGINE TOOLING + BIM ROUND-TRIP + RECT-DUCT + VOXEL-VECTORIZER-1 + DRAINAGE. #1 SPATIAL HARNESS / Delta G (library/harness/spatial-harness.mjs, + .example.mjs worked proof) — the high-level spatial TOOL SURFACE the AI drives instead of raw code: SCENE/SPATIAL/QUERY-senses(objectsWithin/whatIsAbove/whatIsBelow/pathFree via slab-clip/connectedTo)/CREATE/TRANSFORM/ANCHORED(placeNextTo/placeAgainstWall)/CONNECT(connectPorts by port IDENTITY, RULE006)/VALIDATE/CONTEXT(whereAmI propioception)/LOAD(fromScene). Guards RULES 001-010 — illegal state impossible BY CONSTRUCTION (a colliding placement returns free-space SUGGESTIONS, never an overlap); clearance tunable warn/block (matches verify.mjs SOFT); composes with clash-detect for mesh-exact narrow phase; dependency-free. The library realization of the doc's 'AI proposes via tools, engine OWNS coordinates' thesis + the multi-agent ReserveEngine (E4-evidenced). The @modelcontextprotocol SERVER wrapper stays a separate user-scope decision (working prototype exists). #2 OCCUPANCY GRID (parts/occupancy-grid.mjs) — deterministic semantic voxel grid (FREE/OCCUPIED/CLEARANCE/RESERVED/STRUCTURE/HVAC/PIPING) + atomic reserve/lock at grid resolution (grid-level twin of the ReserveEngine, the spatial engine's second memory). #3 MEP CONNECTORS (harness/mep-connectors.mjs) — validateConnectors: REPORTS-only QC that each connection is SOURCE→SINK within ONE fluid system (CHW↔CHW, never CHW↔HHW) + DN-mismatch warnings; catches 'chilled tied to hot' before geometry. #4 BIM ROUND-TRIP — ifc-export (scene_graph→IfcDistributionElement/Port/RelConnectsPorts: CONNECTIVITY not just adjacency) + ifc-connectivity (the READER; export→read round-trip proven, reads hand-written IFC too); pure-Node/offline (flat STEP text, no web-ifc WASM to write/read connectivity), NODE tools NEVER bundled into the browser dist, never [CERT]. #5 RECT-DUCT (parts/rect-duct.mjs) — RMF-swept RECTANGULAR HVAC duct: rings a box cross-section along the twist-free RMF frames so a real box trunk never spirals through bends (the doc's rect-sweep-twist insight); the box counterpart to rmf-frames swept tube. REVIEW CAUGHT a real inside-out wall-winding bug (signedVolume<0) my twist-free test missed — fixed + a winding regression added (dogfoods geom-metrics), the QC modules earning their place. #6 DUCT-VECTORIZE increment-1 (parts/duct-vectorize.mjs) — classifyDuctJunctions: types where duct runs meet (collinear-same→straight, collinear-diff→reducer, angled→elbow+deflection turnAngle=acos(-dot), 3→tee, 4→cross, 1→free-end); the voxel→CAD 'keep intent not cubes' bridge feeding rect-duct + fitting-select + hvac-fittings. #7 DRAINAGE (parts/drainage-slope.mjs applyDrainageSlope + harness/drainage-check.mjs) — per-run continuous-slope transform + a pooling-defect gate, closing the v1.18 caveat (orthogonal routing concentrates the drop → flat runs pool). Remaining v1.19 increments land as v1.19.x redeploys: voxel-occupancy→runs skeletonization (feeds duct-vectorize), scene_graph element-typing, occupancy↔engine wiring. 1.18 (2026-08-26, user-directed multi-session research effort — 6 sessions applying investigacion.md + investigacion2/3/4.md audits; deltas validated by creador PoCs, all library tests green): NET-NEW GEOMETRY + ROUTING/CLASH + SPATIAL-ENGINE + MERGED GATE. #1 EXACT-ARC HVAC FITTINGS (library/parts/hvac-fittings.mjs) — the doc's #1 geometric requirement: real HVAC elbows are EXACT constant-radius arcs (centerline on x²+y²=R²), which the existing rmf-frames spline sweep CANNOT emit (MEASURED spline drift 0.093m = R·(√2−1) = 41%-of-R overshoot 'melted pipe' + a degenerate torn mesh). elbow via three-core TorusGeometry arc param (ZERO new dep), reducer, tee; pure Node-testable core (elbowCenterline/elbowPortFrames/reducerProfile/elbowPlacement) + async builders (makeElbow/makeReducer/makeTee/elbowAtBend). elbowAtBend is the ROUTER→FITTINGS bridge — consumes a duct-router bend {position,inDir,outDir,turnAngle}, places+orients an exact-arc elbow with tangent setback T=R·tan(θ/2) (proven to ≤2.8e-16 in the full-chain PoC). 53/53 tests. #2 ROUTING + CLASH + TOPOLOGY (inv3 MATHQC, creador-PoC-validated) — parts/duct-router.mjs (pure-JS zero-dep 3D A*, state=(cell,dir) turn-penalty cut bends 5→2 at identical length; emits bends metadata → elbowAtBend; toOrthogonalSegments for ductwork — spline bow eats reserved clearance, reserve ≥1.3·gridStep if rounded); harness/clash-detect.mjs (headless three-mesh-bvh interference gate; gate on interior penetration depth>tol not the raw boolean; sampled ray-parity depth — the AABB proxy is placement-dependent, fabricating 0.123m on a non-contacting 45° corner; NODE gate tool, NEVER vendored into the offline browser dist); harness/geom-metrics.mjs (surfaceArea/centroid/volumeMetrics — signed-volume≤0 inverted-winding discriminator). #3 SPATIAL/CLASH/MECHANICAL GATE MERGE (GATES.md, one section EXTENDING blind-review + ΔE00) — clash ORACLE (reports never repositions) + rules 001-010; mechanical HARD-FAILS (CriticalClashes/DisconnectedPipes/InvalidGeometry/OutOfBounds=0) CAP the score at 0.79; BEST_VERSION=max(Q1,Q2,Q3) + Self-Refine; 8-view μ−λσ consistency (ADVISORY, not acceptance); Monte-Carlo P(clash) opt-in via clearanceRisk.pClashMax; topology-gate wiring; auto perf-budget (maxDrawCalls/maxVisibleTriangles); proxy→realistic TRANSFORM-PRESERVATION invariant. All follow the declared-but-missing=fail-loud / absent=byte-identical superset discipline. #4 SPATIAL WORLD MODEL (references/spatial-world-model.md NEW + DESIGNSPEC scene_graph/upAxis/clearanceRisk/keyframes) — external engine OWNS coordinates (AI never writes raw XYZ), reserve→validate→commit→snapshot, volumes-not-points, CONTACT vs ILLEGAL_COLLISION. S2 verdict MEASURED across 4 controlled A/Bs: FALSE single-agent (A1 N=4, A2 N=12 both null — naive opus = engine), quality-improver single-agent-with-obstacle (A3 soft: naive ate clearance, engine reserved it), REQUIRED multi-agent (E4 hard: two blind agents' primaries collide → naive-parallel FAIL vs ReserveEngine-lock PASS — an observability limit). Label: guarantee+clearance-awareness single-agent, required multi-agent. Working reference impl + F1 end-to-end flow (9.5) prove it executable. #5 three.js is NOT a CAD kernel (TRACK-THREEJS boundary note) — OCCT-WASM 48.9MB/7.1MB-min fails the offline-single-file contract (assertNoNetwork) → OPTIONAL online-only research track, never a bundled dep; Chili3D AGPL. All rival ENGINES (Godot/Bevy/Babylon/PlayCanvas/A-Frame/Filament/Cesium) REFERENCE-ONLY (no migration); Filament PBR spec cited for the materials pass. #6 ASSET RULE-8 TIGHTENED — AI-mesh generators (Hunyuan3D/Rodin/TRELLIS) BLOCKED from the CERT track (MIT code ≠ MIT output; territorial exclusions). INTEGRITY: the doc invents repo names that don't resolve (SCRIPT3D/ImperativeScene/Compos3D) — kit ships only verified citations. 6 LEARNINGS §Staged rows (research-verify vocabulary flagged for user). v1.19 candidates staged (NOT applied — need own design+build): MCP tool surface (working prototype exists), voxel→CAD vectorizer, IFC-export track, RMF rectangular-duct builder. Consolidated: scratchpad-research/PROPOSAL-v1.18.md + NEWDOCS-FINDINGS.md. 1.17 (2026-08-21, user-directed, applied the 2026-08-21 COB-IM2 retro deltas — 5 retros under disenos/COB-IM2/runs/): #1 CAD→3D TWO-PHASE ARCHITECTURE — Phase 1 vanilla three.js offline single-file (reconstruction, offline is a DEV constraint) → Phase 2 React+React-Three-Fiber dashboard (product), certified JSON data layer DECOUPLED and reused via <primitive> so Phase 1 is not throwaway (references/TRACK-THREEJS.md §Generic defaults). #2 R3F TRACK GOTCHAS — per-frame animation in useFrame on the three objects (damp + userData) never React state; drei <Html> labels not canvas sprites; Next.js needs dynamic(ssr:false) + explicit parent height or the canvas silently collapses to blank; an R3F component CANNOT preview as a Claude artifact (CSP blocks esm.sh) → ship a browser-only esm.sh+Babel HTML preview (references/TRACK-THREEJS.md §Generic defaults). #3 CAD→3D INTAKE RANKING — Route 1 (default, certified): read real entities via a DWG reader ladder (system libredwg dwg2dxf → ODA File Converter → compile libredwg from source → ezdxf); sheets sharing one CAD frame co-register by pure TRANSLATION, content-verified. Route 2 (last resort, all [INFER]): CV-on-raster (colour masks + HoughLinesP + connected-components, SIFT/Lowe/RANSAC co-registration) — invents scale/elevation/semantics/co-registration (a SIFT similarity reports a spurious scale the CAD does not carry); embedded DWG thumbnails ~256×115 are unusable (references/PIPELINE.md §Triage). 3 LEARNINGS rows promoted into §Active. Evidence: COB-IM2 corpus B12 (dwg2dxf == dwgread == ODA on modelspace payload) + dwgbmp thumbnail 256×115 + B4 translation-only co-registration. 1.16 (2026-08-21, user-directed, asset-sourcing-kit-delta.md + nave-v9 D18): #1 MESH LICENSE GATE HARD RULE — never download or use an asset whose license is unknown or more restrictive than CC-BY; license MUST be recorded before first use (Rule 8 tightened). #2 CATALOG-FIRST SOURCING — before sourcing any external mesh check the project's own catalog (disenos/catalog/) first then source from CC0-first public repositories; discovery step added to Execution Step 0. #3 MESH PROVENANCE SCHEMA — per-mesh provenance record fields added inline to Rule 8 (url · author · license · sha256 · scale · transforms · triangle/vertex count · fidelity note). #4 NEVER [CERT] tightened — added 'at most [INFER]' to Rule 8 (certainty attaches to what we verified, not to what we fetched). #5 DETERMINISTIC CAPTURE HOOK — window.__cam accessor + --cam <x,y,z,tx,ty,tz> flag for reproducible headless QA captures added as Rule 9. #6 OFFLINE BUILD RECIPE — ALREADY-PRESENT (v1.13 TRACK-THREEJS §Delivery kit + PIPELINE P7 with assertNoNetwork covers this). 1.15 (2026-08-16, user-authorized application of disenos/nave-panccadia/runs/2026-08-16-video-additions-retro.md — 8 kit deltas + 9 LEARNINGS promotions): #1 colorTarget SOURCE RULE — anchor srgb to the source photo/video crop, not a render; golden-render fallback is REGRESSION DETECTOR only (DESIGNSPEC.md §Schema + GATES.md §Objective material-colour gate). #2 gate_passes NESTING TRAP — MUST be top-level; nested value is invisible and the asset never closes green (DESIGNSPEC.md §Schema). #3 NATIVE-RESOLUTION VIDEO INTAKE — contact-sheet thumbnails (~300 px) misread metalness/colour; extract hi-res frames via video-inspect.mjs before authoring material calls (DESIGNSPEC.md §Reference-image intake). #4 FRAMING-PROBE OCCLUSION caveat — fullyVisible checks NDC projection, not physical occlusion; a blank-wall capture can still read framing: ok; extend VIEW_HIDE to every opaque layer between camera and subject (GATES.md §Mechanical checks). #5 TOGGLE-AWARE MERGE — keep each toggleable sub-group in its own merge pass; an absorbing merge destroys the toggle's geometry (TRACK-THREEJS.md §Pass ladder optimization row). #6 CAD-PLACEHOLDER PRE-CHECK — before placing a master into a generated scene, filter for existing CAD/furniture placeholders and FAIL LOUD if filter suppresses zero matches (PIPELINE.md §Triage). #7 SCENE-VS-MASTER RIG — dark-metal in the integrated scene may be systemic (rig) not asset-specific; measure by comparing against an existing metal in the same scene; a systemic problem requires a GLOBAL rig change surfaced to the user (TRACK-THREEJS.md §Generic defaults). #8 L-SECTION OPEN-QUADRANT SPAN — span to the sheet face, not the bbox; open quadrant floats 20–35 mm; assert overlap depth at both ends (DESIGNSPEC.md §Authoring rules). 9 LEARNINGS rows promoted from §Staged into §Active (8 promoted-fixed, 1 new). 1.14 (2026-08-15, user-directed, applied on explicit instruction — not a design-run self-integration): CAPTURE-GC FULL-LADDER SUPPORT + CORRECTED PRUNE POLICY + SWEEP-CAPTURES TOOLBELT. GAP 1 fixed: `assets/capture-gc.mjs` no longer SKIPS design dirs with no canonical `<slug>.png` (full-ladder designs, e.g. disenos/homelab/rack-cabinet). PRUNE step now runs with the corrected EVIDENCE vs EPHEMERAL policy: (A) ALL frames of FAIL-attempt bases (review present, verdict ≠ PASS) are prunable — their review.jsons kept; FAIL-attempt PNGs are not gate-load-bearing (gate-state reads only the PASS basename). (B) Suffixed png of a passBase whose md5 == rep png is a byte-twin → prunable; distinct-view frames (md5 ≠ rep, e.g. attempt3-glass/open) are EVIDENCE cited by the review → KEPT. (C) No-review-owner frames → prunable. PROMOTE step is flat-catalog-only (skipped for full-ladder). Flat-catalog path byte-identical. 12-test characterization suite (`tests/capture-gc.test.mjs`, fixture `tests/fixtures/full-ladder-gc/`), 12/12 green; prior 21/21 gate-state green → total 33/33. Validated dry-run on disenos/homelab: 24 prunable files / 11.24 MB (vs 0 before), with attempt3-glass/open correctly KEPT as evidence. GAP 2 fixed: `assets/toolbelt/sweep-captures.sh` + `sweep-captures-hook.sh` (mirrors sweep-retros pattern — fail-loud zero-census, SessionStart hook, D3_DESIGN_REPO env). Documented in GATES.md §Capture cleanup. Hook not auto-wired. 1.13 (2026-08-15, user-directed and applied directly on the user's explicit instruction — NOT a design-run self-integration, so the §Hard-boundary propose-only rule does not apply): MODULAR-SRC DEFAULT + OFFLINE SINGLE-FILE BUILD. TRACK-THREEJS §Generic defaults — NEW threejs designs are authored MODULAR (an index.html shell + a main.js entry importing a src/** tree split scene/ sim/ dashboard/ controllers/ dev/; template precedent disenos/nave-3sistemas/); dev previews and gates run over the harness http server because ES-module FILES do not load from file://, so a modular design opens by double-click only via its P7 dist/, never during the run; the ~129 legacy single-file monoliths are GRANDFATHERED, never migrated wholesale. TRACK-THREEJS §Delivery kit + PIPELINE P7 — a modular design emits a self-contained dist/index.html via the NEW repo tool research/tools/build-offline.mjs (generalized from disenos/datacenter-hotspot-sinCDN/build-offline.mjs to handle an external main.js entry over a src/** tree; esbuild bundles main.js + src/** + vendored three/addons/lil-gui — aliases derived from the page importmap — into one inline-module HTML, inlines the external CSS, strips the importmap, and assertNoNetwork fails loud on any surviving remote dep); the dist is a deliverable MIRROR of the gated modular source (verify visual parity vs the gate capture), never a second source of truth, never hand-edited. PILOTED end-to-end on nave-3sistemas: build ~5 s, 28.6 kB entry -> 766.6 kB page; headless file:// load rendered the full dashboard with data-app-ready=true, WebGL context ok, ZERO network requests, clean console, and the __qaFraming hook preserved (dist stays gateable). Draco explicitly REJECTED as the opening mechanism — it compresses .glb mesh geometry (a size trim, not a load mechanism), 283/285 designs are procedural with no mesh to compress, and an external GLB + DRACOLoader are file:// fetches that make double-click open WORSE, not better. 1.12 (2026-08-15, user-delegated application of the nave-3sistemas P8 retro, disenos/nave-3sistemas/runs/2026-08-15-retro.md): PROMOTED (on the user's explicit 2026-08-15 instruction) 6 rows into §Active — authored here from the P8 retro deltas + disenos/catalog/EXTERNAL-ASSETS.md; they passed this session's working-tree §Staged review but were never committed to the kit's §Staged, so there is no in-kit staged original to diff (an independent reviewer verified the content against those true sources): offline-first shell default, the external-mesh track, hideable dev-UI (lil-gui) with JSON export, re-gate-the-WHOLE-evidence-set after any change, escalate-to-PANEL when a feature has BOUNCED across attempts (single-judge margin variance), and differentiate co-located services by CROSS-SECTION not material alone. APPLIED the retro's three P8 deltas: GATES.md §Self-correction loop — after ANY source change re-CAPTURE + re-GATE the whole evidence set, never only the changed feature (luminaire-grid PASS 0.76 → FAIL 0.55 with geometry unchanged); GATES.md §Blind-review protocol — escalate the FINAL verdict to a 2/3-judge blind PANEL when a feature BOUNCES across attempts (not only after N same-feature failures; luminaire-grid 0.76↔0.55, ahu-and-duct-run 0.76↔0.71 single-judge → panel unanimous 0.79); TRACK-THREEJS §Generic defaults — differentiate co-located services by CROSS-SECTION/shape (round HVAC trunk beside round air pipe failed 0.71 → rectangular box duct passed). REGISTERED offline-first as the TRACK-THREEJS §Generic-defaults default (vendor/three/ + local importmap, no run-time CDN, verify no http(s):// survives; precedent disenos/datacenter-hotspot-sinCDN/) and the external-mesh track as SKILL Hard Rule 8 (concise pointer to disenos/catalog/EXTERNAL-ASSETS.md — CC0/CC-BY GLB, normalize 1u=1m + embed, license+provenance+load+no-external-URIs+scale+non-black gate, NOT ΔE00, never a [CERT] subject). 1.11 (2026-08-07, user-delegated chain after the v1.10 colorTarget seeding): ARTIFACT HYGIENE. New tool `assets/capture-gc.mjs` closes the SKILL v1.8 DEFERRED `capture-gc` item — `node capture-gc.mjs <design-dir|catalog-root> [--apply] [--dedup]`: promotes the passing representative's review to the canonical `<slug>.review.json` (the flat catalog copied `<slug>.png/.console.json` but never the review, so the gate verdict + `color_delta_e00` had no canonical home) and prunes scratch frames (dry-run by default). BYTE-TWIN DEDUP (a promoted staged learning): the flat-catalog `<slug>.png` is a byte-identical copy of the passing `<pass>-attempt<N>.png`, so `gate-state.mjs` gained a `hasPngWitness` canonical fallback — the promoted `<slug>.{png,review.json}` witnesses its OWN declared pass, letting `--dedup` prune the redundant attempt-PNG twin without erasing gate history (guarded by `canonWitnessPass`, ABSENT canonical review = byte-identical prior behavior); `canonical-witness` characterization fixture + test added, suite 21/21 green (20 prior byte-identical). CACHE RETROFIT: `gate-state.mjs --write-cache` regenerates a missing `runs/progress.yaml` from the derived ladder, gated on a coherent derivation (no tool wrote progress.yaml before — it was hand-authored, so an original build that skipped it left a permanent cache-missing drift). Applied on nave-panccadia equipos: 18 reviews promoted, 18 byte-twins deduped (7.39 MB freed), 9 caches retrofitted → 18/18 gate-clean. Documented in GATES.md §Capture cleanup + LEARNINGS §Staged rows promoted. 1.10 (2026-08-07, user-directed adoption from the img2threejs upstream, documented in research/threejs-block53.md): OBJECTIVE material-colour gate to fix a MEASURED reviewer-variance failure — two blind reviewers scored the SAME stainless render (mean gray 91.4 vs 91.6) 0.80 PASS vs 0.57 FAIL, so 'attempts per asset' is not a reliable kit-version metric. Adopted CIEDE2000 (dE00) from the upstream (`forge/_shared/color_metrics.py`, Apache-2.0 (c) 2026 hoainho) as the one directly-reusable piece; their target comes from a reference photo we lack, so the target moved to a spec-declared `colorTarget: {srgb, deltaE00Max, crop}`. New repo tool `research/tools/material-color-probe.mjs` (pure-JS dE00 + ImageMagick crop-mean) measures the render crop and records `mechanical.color_delta_e00`; `gate-state.mjs` reads the OPTIONAL `deltaE00Max` (loose YAML, like global_min) and enforces `dE00 <= max` on a materials/surface PASS — declared-but-missing is a contract violation (fail loud), ABSENT = byte-identical prior behavior (pure superset, guarded by `specDeltaE00Max !== null`). gate-state stays dependency-free: the probe measures at review time, the deriver only enforces the recorded number. Documented in DESIGNSPEC.md §Schema + GATES.md §Verdict + review.schema.json (`color_delta_e00`); 3 characterization fixtures + tests added, full suite 19/19 green (16 prior byte-identical). Validated end-to-end on the benchmark renders: dE00(v1.9-recipe vs v1.8-PASS golden)=0.14 PASS (collapses the reviewer-variance false-negative) while dE00(v1.9-spec-material vs same)=11.69 FAIL (catches a real spec-vs-recipe material divergence). The roughness/gloss 'reads metallic' axis is NOT solved by this and stays with the reviewer. 1.9 (2026-08-07, user-delegated application of the nave-panccadia equipos heavy /loop retro, disenos/nave-panccadia/equipos/runs/2026-08-07-retro.md): applied retro deltas #1-#4 (NOT #5). #1 (HIGH, flat-catalog gate) — gate-state.mjs honors an OPTIONAL, EXPLICIT `gate_passes:` spec field (a declared pass-subset, e.g. `gate_passes: [materials]`); when present the derivation ladder becomes exactly that subset in canonical order (unknown name = hard error, passes outside it absent-not-locked, progress.yaml only expected to cover the subset), ABSENT = the full 8-pass ladder byte-identical (pure superset); complexity.tier auto-derivation deliberately NOT taken. Documented in PIPELINE.md §Triage (flat-catalog shape) + GATES.md §Verdict + DESIGNSPEC.md §Schema; characterization test added, full suite 16/16 green (15 prior byte-identical). #2 (HIGH, TRACK-THREEJS §Generic defaults) — headless-safe bare-metal recipe: env intensity 2.15 + HemisphereLight + FRONTAL DirectionalLight fill + brushed roughnessMap, the headless complement to the §3.3 RectAreaLight studio variant that stalls SwiftShader. #3 (MED, TRACK-THREEJS §Generic defaults) — `OrbitControls.autoRotate` DEFAULT OFF for gate captures (a rotating scene never settles → false `console_clean:false`). #4 (MED, PIPELINE §Triage) — a flat/catalog run owes a per-asset (or run-end) library-extraction checkpoint since it never reaches P7. 4 library extractions registered: brushed-stainless-recipe + transmission-free-glass (materials-textures/), stainless-equipment-shell template (harness/), gooseneck-spout (parts/) — each with a source file:line pointer + materials-PASS gate evidence. LEARNINGS: 4 rows promoted into §Active (deltas #1-#4) + 1 kept §Staged (delta #5, capture network-retry — not applied); the 6 compresor-tornillo-aire 2026-07-22 §Staged rows that were uncommitted in the working tree before this session are NOT part of this commit — left dirty for the compresor retro's own adjudication. 1.8 (2026-07-20, user-delegated adjudication of the cinemex runs/2026-07-13-retro.md §Appended live 2026-07-18 section — the lone row a stale marker flagged, applied 2026-07-16 marker predating 07-18 content): APPLIED the capture-GC delta as an EXTENSION of the existing GATES §Artifacts on-gate-close rule (the 2026-07-12 deletion rule proved insufficient — measured 432 PNGs / 285 MB, only 28 bound by exact name). GATES §Artifacts now defines the EVIDENCE-vs-EPHEMERAL dichotomy (ephemeral frames route to scratchpad, never runs/assets/) + a capture-GC step at every round/pass close that reports the census (kept/deleted/bytes) in the run note and deletes orphans, with the canonical `<pass>[-l<L>]-attempt<N>.*` gate names protected because gate-state derives pass state from them. 1 promoted row (LEARNINGS §Active current schema). Process note: the 07-18 append skipped its at-append §Staged row per the queue-gap rule and its marker was never reset to pending — folded directly on adjudication, traceability recorded in the promoted row. DEFERRED as engineering: a toolbelt `capture-gc.sh` sweep script (mirrors sweep-retros.sh) — the rule is binding now; the automation is a later kit-maintenance unit. 1.7 (2026-07-16, research-sdd toolbelt comparison + gate-state fixtures round, user-delegated): TOOLBELT installed at assets/toolbelt/ — verify-kit-clean.sh checks BOTH kit repos (design3d + anti-ai-ui; accepted-but-uncommitted work found in both the same session) and sweep-retros.sh sweeps disenos/*/runs/*-retro.md with STALE-MARKER detection (appended-live sections postdating an applied/dismissed marker) + fail-loud zero-file census, both with SessionStart hook wrappers + shared lib/retro-status.sh. SELF-IMPROVEMENT: queue-gap fix (a live append writes its LEARNINGS §Staged row AT APPEND TIME; an append to an adjudicated retro resets the marker to pending) + concrete `Retro:` commit trailer for traceability. GATES §Gate steps 2: a demoted flag carries a DEFINED EXIT CONDITION — --gpu graduates via the per-content-class pilot. LEARNINGS §Entry format: graded certainty markers [measured] > [session-observed] > [inferred] (existing informal prefixes grandfathered). gate-state.mjs: PASS with NO resolvable global_min (no spec, no assumed_thresholds) now flags as a contract violation instead of silently skipping (turns the fixtures round's intentional RED test green), and the zero-evidence all-locked state says 'no gate evidence yet' instead of the verified-clean wording; 15-test characterization suite over synthetic fixtures (tests/) — first exercise of the Blender ladder path — regression-verified byte-identical on cinemex + damper live derivations. 2 promoted rows (gmin silent-skip; kit-accepts-then-commits w/ verify-kit-clean backstop) + 1 staged row (what a Blender reviewer truthfully records for console_clean/budget_pass — no console/probe exists on that track). 1.6 (2026-07-16, user-delegated adjudication of the cinemex runs/2026-07-13-retro.md §Appended live sections): applied D2 (qualify a renderer per content class before it produces gate evidence — GATES §Gate steps 2; --gpu now experimental-not-gate-safe), D3 (a watchdog's liveness pattern excludes the watchdog itself — PIPELINE §Execution modes + GATES §instrument corollaries), D4 (the judge output contract is pasted VERBATIM in every reviewer brief, never a schema pointer — ROLES §render-reviewer), D5 (post-refine-spec SCOPED P3 — GATES §Verdict + PIPELINE P3 row), D6 (one writer owns the tree; scope extends via numbered pre-diagnosed extensions — ROLES §Fan-out); `improvised-win` added to the LEARNINGS `how` vocabulary (all five prior values were failure modes — D5/D6 are wins, not catches). D1 (no gating command behind a pipe in an && chain) NOT re-applied: already in PIPELINE §Execution modes via an UNVERSIONED 2026-07-15 edit (mtimes read 2026-07-16 before they were consumed — recorded then, not re-verifiable); logged for the traceability it skipped. REJECTED: preflight/capture double-handling (the retro declares its own measurement debt). DEFERRED as engineering: gate-state.mjs fixtures + tests. 1.5 (2026-07-14, user feature): reusable-block LIBRARY (library/LIBRARY.md contract: check-before-building, extract-after-gating at P7, evidence-or-no-entry; library/INDEX.md registry seeded with 30+ blocks from cinemex, hotel voxel/realista and cuarto-frio-safran incl. the sims-floating-banner; extraction-traps guide) + SKILL step 0 + PIPELINE P7 extraction step. 1.4 (2026-07-14, user-delegated adjudication of the cinemex retro): applied deltas #4b (gate evidence at --dpr 3), #6 (LINEAGE RESET first-class in GATES), #12 (coverage check in gate step 0), #13 (DEFERRED-CORRECTIONS ledger rule), #14 (canonical representative copy), #15 (pass-label on locked→active), #16 (fail-loud input census), #17 (P7 harnesses are repo tools) + GLTFExporter caveats + measure-the-artifact/unit-conversion corollary; 6 staged learnings promoted→folded (see LEARNINGS §Active provenance); gate-state.mjs hardened per audit D2/D7 (track-aware ladder selection via anim-rig/optimization-export detection — blender path untested until a blender run exists — and PASS reviews now checked against spec quality_contract.global_min with assumed_thresholds fallback; regression-verified clean on cinemex 8/8 + damper 8/8). 1.3 (2026-07-14, user-authorized mid-run): retro deltas #3/#7/#8/#11 + audit fixes S1-S5/S10."
---

# design3d launcher

Thin launcher for the dual-track 3D design pipeline. The kit under `references/` is the single
source of truth — never improvise phases, gates, or track rules from memory.

## Activation Contract

- `/design3d <design> [threejs|blender] [new|continue]`, or any free-form request to create,
  iterate, or review a 3D model/equipment/scene.
- `/design3d <design> status` → run `assets/gate-state.mjs <design-dir>` and report its output.
- Resuming any design directory that contains `design-spec.yaml`.

## Hard Rules

1. Before any work, read `references/PIPELINE.md`, the active `references/TRACK-*.md`, and
   `LEARNINGS.md` **§Active ledger ONLY**. Active entries at `confirmed` or above are BINDING for
   the run. **NEVER read §Staged as authority** — it is the user's review queue, not a rulebook.
2. NO geometry before an approved DesignSpec — including animation-ready hierarchy with named
   pivots and 3–5 critical features, hard max 5 (`references/DESIGNSPEC.md`).
3. Pass-locked pipeline: advance only when the pass gate PASSES (`references/GATES.md`). A
   critical feature below its threshold FAILS the pass even with a high global score. Max 2
   correction retries per pass, then STOP and present evidence.
4. Multi-asset scenes: build ONE asset at a time, gate each before the next.
5. WSL2/no-GPU environments: NEVER screenshot WebGL via chrome-devtools MCP. Capture only via
   the track's harness (`capture.mjs`) over a local http server — never `file://`.
6. Never edit this kit mid-run, and **NEVER integrate your own lessons**. The P8 retro PROPOSES kit
   deltas AND STAGES LEARNINGS entries (`references/SELF-IMPROVEMENT.md` §Hard boundary — propose,
   never apply). **Nothing a run writes is binding until the USER promotes it.** A run may write to
   `LEARNINGS.md` §Staged and nowhere else; it may never promote a row into §Active.
7. Test-vs-render jurisdiction (`references/GATES.md`): if a human can judge it by looking at one
   image, RENDER it; if a human cannot count it at a glance, TEST it. Never simulate the renderer
   inside a test to predict a pixel.
8. **External-mesh track** (a THIRD track beside procedural-direct / needs-research):
   reuse/download a CC0 or CC-BY GLB — **NEVER use an asset whose license is unknown or more
   restrictive than CC-BY; the license MUST be recorded before first use.** Normalize to 1u=1m
   + embed textures (self-contained/offline), and gate it with a
   license+provenance+load+no-external-URIs+scale+non-black check — NOT the ΔE00 gate.
   Per-mesh provenance record: url · author · license · sha256 · scale · transforms ·
   triangle/vertex count · fidelity note — so origin and terms are auditable at any time.
   An external mesh is never a `[CERT]` measured subject (at most `[INFER]` — certainty
   attaches to what we verified, not to what we fetched).
   **AI-GENERATED meshes (Hunyuan3D · Rodin/Hyper3D · TRELLIS/TRELLIS.2) are BLOCKED from the
   external-mesh CERT track — tier-C decorative props only, NEVER certified.** A permissive (MIT)
   CODE repo does NOT license the generated mesh OUTPUT; "no rights claimed" is NOT a CC0/CC-BY
   grant; several carry extra terms (Hunyuan3D EU/UK/Korea territorial exclusions; Rodin free-trial
   output license unknown). Only Poly Haven (CC0) is a clean auto-source; Sketchfab is per-model
   manual license capture. The block is specifically the NEURAL generators above (trained on
   unlicensed data). A PROCEDURAL generator (e.g. Infinigen, BSD-3-Clause) is a WEAKER case — its
   output carries no training-data provenance risk and MAY be used as a tier-C prop with provenance
   `procedural output, no explicit grant` (`[INFER]`, never `[CERT]`). Do NOT blanket-block a
   procedural source, and do NOT treat a neural generator as clean. Full spec:
   `disenos/catalog/EXTERNAL-ASSETS.md`.
9. **Deterministic capture hook**: for reproducible headless QA, every Three.js scene must
   expose a `window.__cam` accessor and honour a `--cam <x,y,z,tx,ty,tz>` flag so captures
   are taken from an identical, scripted viewpoint — never an interactively-nudged one.
   Pixel-diff comparisons are only valid over identical viewpoints.

## Decision Gates

| Fork | Rule |
|---|---|
| Track | Blender named or Blender MCP connected → blender · repo has `disenos/` or importmap HTML prototypes → threejs · else ask once |
| Mode | single tweak on existing design → quick · one new design → standard · scene or ≥3 assets → heavy (auto-escalate; announce, don't re-ask) |
| New vs continue | `design-spec.yaml` exists → continue (reconcile live state first) |

## Execution Steps

0. **Check the reusable-block LIBRARY** (`library/INDEX.md`) before authoring any mechanism or
   part it already covers; extract newly gated reusables back into it at P7
   (`library/LIBRARY.md` — check-before-building / extract-after-gating / evidence-or-no-entry).
   For external mesh sourcing: check the project's own asset catalog (`disenos/catalog/`) first;
   only then source from CC0-first public repositories.
1. Discover project overlays per the active `references/TRACK-*.md`; record paths (not copies) in the spec.
2. Reconcile live state: `design-spec.yaml` + `runs/progress.yaml` + engram `design3d/{design}/*`.
3. Run the P0–P8 DAG per `references/PIPELINE.md`, gating every pass per `references/GATES.md`.
4. Produce the delivery kit (P7, track-specific).
5. Run the retro (P8) per `references/SELF-IMPROVEMENT.md`.

## Output Contract

Delivery kit artifacts + result contract (`status`, `executive_summary`, `artifacts`,
`next_recommended`, `risks`) + retro written (`review-status: pending`) and LEARNINGS entries STAGED
for the user's decision — never appended to §Active by the run.

## References

`references/PIPELINE.md` · `references/DESIGNSPEC.md` · `references/GATES.md` ·
`references/TRACK-THREEJS.md` · `references/TRACK-BLENDER.md` · `references/ROLES.md` ·
`references/SELF-IMPROVEMENT.md` · `references/spatial-world-model.md` · templates in `assets/`
